# Character — frontier (the maturity ledger)

The fourth axis. The other three say *what is claimed* ([`character.md`](http://character.md)), *how it is derived* ([`character_receipts.md`](http://character_receipts.md)), and *what is borrowed* ([`character_prior_art.md`](http://character_prior_art.md)). This one says **how settled each thing is, and exactly what would move it** — the single source of truth for everything not yet load-bearing. Its job is not to feed the core but to keep provisional work *out* of it until it earns in: the structure here is quarantine, not on-ramp. The value of `character.md` is that everything in it is committed; that guarantee survives only if the line below is strict.

---

## Dashboard — scannable index *(get caught up here; the full entries, with ↑ promote / ✗ kill / → target, are below)*

*The full index — one row per entry, grouped by rung. Scan for your substrate or question, then drop to the dense entry below for the verdict, the promote-gate (↑), the kill-condition (✗), and the core target (→). This is a **navigation layer on top**: nothing below is moved or deleted; every entry is preserved verbatim. The table is lossy by design (one-line verdicts), the doc lossless. Rungs:* `steeping` *(speculation) →* `sharpening` *(a mechanism or test exists) →* `battery` *(a runnable falsifier) →* `staked` *(planted in the core, un-instanced) →* crossed *(now in* `character.md`*); plus* `nudges` *(reader-steering) and* `tombstones` *(killed, kept so they are not re-tried).*

### Steeping

| entry | one-line verdict |
| :---- | :---- |
| `flow-resident-number` | a number-type whose identity is a non-terminating flow-trajectory, not a static value |
| `fractional-operator-algebra` | an operator algebra on memory regimes β∈(0,1\], recovering the integer-β operators at β=1 |
| `two-reframe-parallel` | the Boolean limit (D→∞) and the marginal point (ε→1) share architecture — is there a third axis? |
| `secondary-regime-operators` | damping / phase-lock / coarse-graining trichotomies as further operator-algebra candidates |
| `post-threshold-universality` | a candidate mechanism for the seam-width law — none found yet |
| `palm-self-probe` | the two-frame iff-chain re-projected into queueing (time-stationary vs Palm / event-conditioned average) |
| `epsilon-hub-transport` | one transport law on the distance-to-marginal ε, read across four registers |
| `affinity-hub-transport` | one transport law on the affinity a, read across three registers |
| `predictor-soft-sector-blindness` | a feedforward predictor is confident-and-false on hard-sector questions (the neither corner) |

### Nudges *(reader-steering; HELD)*

| entry | one-line verdict |
| :---- | :---- |
| `nudge:loaded-endpoint-descent` | read a loaded phenomenon (sleep, a market, a mood) by descent to its substrate skeleton — periodicity ≠ the protected bit; only the protection test certifies it |

### Shapes *(cross-track recurrence classes; navigation)*

| shape | one-line |
| :---- | :---- |
| `shape:apparent-structure-not-observable` | apparent structure (periodicity/capability/figure/endpoint) ≠ the protected bit; the dual: don't over-credit *either* pole — the gradient carries the bit. Recurrence fired (2026-06-13) → `nudge:loaded-endpoint-descent` HELD review due |

### Sharpening

| entry | one-line verdict |
| :---- | :---- |
| `seam-classification` | every deformation-calculus threshold is one seam via singularity theory (singular set + codimension + universal unfolding); the rounding exponent & hard-vs-soft are the normal form; metric→singularity→topology — the protected sign = the non-unfoldable stratum (**crossed**) |
| `dimensionless-substrate` | the dimensionless self-probe ruler, instanced on rock-paper-scissors; needs a 2nd real substrate |
| `consolidation-ascent` | one heat-tax mechanism, three faces (offline duty cycle / compression vs maintenance / tower ascent \= consolidation); **survivable only at β\<1, locked to the β-collapse (burstiness \= the queue-tail face of β); candidate substrate \= a sleeping nervous system; consolidation \= thermalized→external-rewiring conversion; limit object \= a cyclic character** |
| `frustration-ascent` | the generative-recursion bet — generative of chirality/topology (mintable), parasitic on the drive (amplitude autonomy supplied, not minted); evaded by a two-level cascade |
| `self-referential-closure` | the alive-loop operation stack: minting (HAVE) → **templating** (can a circulation bias sign(𝒜) of the bits it mints toward its own? — **MEASURED degenerate 2026-06-19**: faithful 1-bit kernel, von Neumann channel `I` saturates at `C≈1 bit ≪ k`) → **self-wiring** (the recursion writes its own coupling graph; minting goes endogenous — owed, bigger); heredity = templating *over* self-wiring |
| `wall-as-type-boundary` | the marginal point is a ladder of normally-hyperbolic plateaus joined by three boundary kinds (ignition / topological flip / closure-loss) |
| `chirality-protection` | the protected object is the gauge-irremovable affinity / graph-flux sign — not the exceptional pair, not an integer charge |
| `chiral-rotor-triad` | a driven chiral-rotor triad whose protected sign is locked to a geometric shape-chirality (coupling forced nonzero by Clebsch–Gordan 2⊗3⊃1); derivation-closed, unrun → calibration; a route to the *physical* 2nd instance `chirality-protection` owes |
| `cascade-ledger-split` | the conjugate cascade's two ledgers are one coarse-graining entropy-production split (σ\_tot \= σ\_res \+ σ\_hid) |
| `qec-transverse-decomposition` | **the TDT** — metric⊥topological computed in the surface code \+ glass \+ triad: the protected current is barred from *all* metric observables; symmetry-protected, not generic |
| `selective-coupling-class` | a 3rd coupling class — hard-sector-rich / soft-sector-poor (passes circulation, suppresses population mixing); two tiers — symmetry-exact (\= core) vs smallness 𝒮\=\|Γ\|/\|V\| (new); payload \= a cascade-depth law N(𝒮); absorbs the retired `cross-stratum-transduction` |
| `curvature-as-coupling-bias` | local geometry (prefactor / EP / leading symmetry-allowed O(𝒜²)) modulates coupling strength but does not generate the protected current; is the surviving 2nd-order coefficient geometric or merely analytic? |
| `cascade-climbing-universality` | cascade-climbing has a universality class (candidate: KPZ); KPZ-class ⇔ driven (𝒜≠0), EW-class ⇔ detailed-balanced — the growth exponent as a soft-sector readout of the hard-sector affinity |
| `sink-extracts-the-work` | the sink is first-class, co-equal with topology: topology paid upfront and inert, conduit is a puppet, work extracted at the cold sink; falsifier \= the sink-sweep (vary sink, hold source, 𝒜 moves) |
| `drive-not-primitive` | 'drive' is reservoir-geometry the system sits on, not an agent (owned charge, borrowed spin); what's owned is a net topological charge — the character-monopole 𝒜 (a curl-source) — and coarse-graining climbs out of the geometry, so the cascade transduces the *reading*, not the character |

### Battery — executable falsifier specs

| entry | one-line verdict |
| :---- | :---- |
| `battery:seam-collapse` | the amplitude/drive-seam scaling-collapse falsifier; target invariant (α, F); needs a real substrate with a measurable FDR |
| `battery:sign-interior` | the protected-sign interior-circulation kill; vindicated on rock-paper-scissors (drive-independent sign, flips only by rewiring) |
| `battery:wall-ladder` | the plateau-ladder falsifier; discriminator \= CLV minimum angle; both faces shown on the Stuart–Landau cascade |
| `battery:dimensionless-self-probe` | the dimensionless self-probe \+ canonical affinity on rock-paper-scissors (closes to 1.7%, noise-independent) |
| `battery:scale-covariant-circulation` | the same circulation at two scales splits into a scale-invariant affinity (the bit) \+ a scale-covariant magnitude |
| `battery:two-survivals-plane` | branch survival (ΔV, basin escape) ⊥ current survival (𝒜) — independent axes, all four corners filled *(crossed)* |

### Staked — planted in the core, un-instanced

| entry | one-line verdict |
| :---- | :---- |
| `staked:gfdr-two-step` | near-threshold FDR is two-step — a short-lag X=1 is not below-threshold |
| `staked:auto-tuning` | a substrate's slow rates auto-tune to the diagonally-stabilising weight W \= diag(γ\_ref/γ\_s,i) |
| `staked:memory-collapse` | the memory exponent β ≈ 1−ε near the marginal point (tracks the contraction modulus) |

### Recently crossed — now in `character.md`

| claim | substrate it crossed on |
| :---- | :---- |
| central commitment · two-frame construction | rock-paper-scissors |
| deformation chart · homochirality | rock-paper-scissors \+ homochiral triad |
| exact two-frame magnitude identity | rotational-OU (forced-not-fitted) |
| composite branch (minting \+ sustained circulation) | fuel-driven DNA reaction network |
| branch survival (ΔV; competitive-exclusion, linear in μ−μ\_c) | homochiral triad (+ twin-cycle 2nd instance via S₂; autocat soft pitchfork) |
| protection re-derived (the protected sign = the non-unfoldable stratum; metric→singularity→topology) | forced composition (Harary graph-flux + `singularity-stratification`); §The two tangent sectors |

### Tombstones — killed, kept so they are not re-tried

| entry | died / replaced by |
| :---- | :---- |
| `flip-cost-across-zero` | protection as a flip-cost across the affinity-zero — boundary-malformed → interior sign-stability test |
| `sign-kill-bisection` | the kill-only standdown — boundary-gated → interior can-kill-and-vindicate battery |
| `thm9-coefficient-series` | Boolean-deviation as a 1/D series — it is a crossover scaling function → (α, F) collapse |
| `alignment-dependent-active-stress` | alignment-dependent active-stress fingerprint — only the flocked correction → alignment-independent fingerprint |
| `quadratic-lyapunov-candidate` | a quadratic Lyapunov form — fails the critical-point test → relative-entropy form |
| `current-aids-escape` | protected current as a barrier-*lowering* resource — gMAM shows ΔV flat in 𝒜 (transverse) → the transverse-decomposition prefactor reading |

---

## Gates — the state machine

Five rungs and a hard line. An item occupies exactly one rung at a time; a rung change is an explicit event with a recorded trigger (no lore promotions).

```
steeping ──▶ sharpening ──▶ battery ──▶ staked ──╮
   │             │             │           │      │  COMMIT LINE
   ▼             ▼             ▼           ▼      ▼  ───────────────
 discarded (tombstone) at any rung      promoted → character.md / receipts / prior-art
```

**Promotion gates** (the condition to cross each boundary):

- `steeping → sharpening` — the speculation acquires *either* a concrete mechanism *or* a way to test it. Pure intuition with neither stays steeping.  
- `sharpening → battery` — a runnable falsifier spec exists: finite operating point, a forced-not-fitted target, explicit kill **and** vindicate conditions.  
- `battery → staked` — the spec is planted in the receipts as a falsifier of record (the `staked` tag), un-instanced by design.  
- `staked → promoted` *(crosses the line)* — requires all of: (a) a **real cross-substrate instance** (a synthetic pass is calibration, never vindication), or for a non-instance claim a **forced-not-fitted derivation**; (b) the move **shrinks** the falsifiable surface, never enlarges it; (c) for a refinement that *defends* an existing claim, a **second independent instance**.

**Demotion / kill gates:** an item whose promotion gate is disproven goes to `discarded` with a one-line tombstone (so it is never silently re-explored). A `staked` falsifier that **fires** does not merely demote — it kills the core claim it defended, and the kill propagates into the core.

**Schema (mandatory).** Every entry carries: `key` **\[rung\]** — *verdict* (current belief, one line) · **↑** promote-gate · **✗** kill-condition · **→** core target.

---

## Invariants — auditable

- **I1 — bridge integrity.** Every `staked` receipt ↔ exactly one frontier entry at `[staked]`. The staked rung is the only place an item is simultaneously in the core and on the frontier.  
- **I2 — no orphans.** Every entry has a `→` target, or is `pure-exploration` (steeping only).  
- **I3 — purity of the core.** Nothing above the line lives in a working file; nothing below it lives in `character.md`/receipts/prior-art. The core contains only what crossed.  
- **I4 — recorded triggers.** No rung change without a logged trigger meeting the named gate.  
- **I5 — self-application.** The framework is itself a dissipative register-system, so its own claims face the filters it applies to substrates. Every `staked`/`promoted` claim must pass three: **collapse** (over-determined — a binding or falsifier, never a single-register identity); **iff-chain** (its architecture is a stated entailment, not an asserted axiom); **flow** (every dynamical quantity moves with the operating point — no inert constant). A claim failing any sits in the framework's own metaphor regime and cannot stake.  
- **I6 — no silently-committed conjectures.** A core claim carrying a falsifier must be in one of three states: its falsifier has a recorded run, it is a forced-not-fitted derivation, or it is explicitly `staked`. A claim with an un-run falsifier and no derivation that is neither `staked` nor on the frontier is malformed and demotes. (This is the gate a boundary-import claim slips when no interior apparatus exists to run it: the antidote is that boundary behaviour is an entailed theorem of the interior, never an un-run import asserted at the limit.)

---

## Register

### Steeping — speculation (no commitment)

* `flow-resident-number` **\[steeping\]** — *verdict:* a candidate number-type whose identity is a non-terminating trajectory under the coarse-graining flow (the affinity-as-orbit, the slow-manifold trace, the running $\\beta$-functions); value-readings are static projections. · **↑** a closed composition algebra under the flow \+ a natural metric. · **✗** no closed algebra. · **→** `pure-exploration`. The algebraic side of the generative-recursion question (paired with `frustration-ascent`).  
* `fractional-operator-algebra` **\[steeping\]** — *verdict:* an operator set on memory regimes $\\beta\\in(0,1\]$ recovering the integer-$\\beta$ operators at $\\beta=1$; the algebra of *coupled* fractional systems is open. · **↑** a closed algebra at finite $\\beta$ \+ a deformation-calculus analog \+ boundary rules at $\\beta\\to1^-$. · **✗** no closed algebra. · **→** a memory-regime operator layer.  
* `two-reframe-parallel` **\[steeping\]** — *verdict:* the Boolean limit ($D\\to\\infty$, static) and the marginal point ($\\varepsilon\\to1$, dynamical) share architecture; watch for a third axis. · **↑** a third concrete reframe axis, instanced. · **✗** the parallel is cosmetic. · **→** `pure-exploration`.  
* `secondary-regime-operators` **\[steeping\]** — *verdict:* damping, phase-locking, and coarse-graining regime trichotomies as further operator-algebra candidates; memory regimes are the cleanest first. · **↑** a closed algebra on any one trichotomy. · **✗** no closed composition. · **→** `pure-exploration`.  
* `post-threshold-universality` **\[steeping\]** — *verdict:* a candidate seam mechanism, no mechanism yet. · **↑** a concrete link to the seam-width law. · **✗** no link survives. · **→** `pure-exploration` (feeds `seam-classification` · the thm9 crossover-width instance / `battery:seam-collapse`).  
* `palm-self-probe` **\[steeping\]** — *verdict:* the two-frame iff-chain re-projected into queueing — the time-stationary average (external) and the Palm / event-conditioned average (self-probe) are an inequivalent pair; Poisson arrivals are the degenerate $X\\equiv1$ case. On a non-reversible cyclic queueing network it reinstantiates: self-probe defined $\\Leftrightarrow$ net circulation $\\ne0 \\Leftrightarrow$ routing affinity $\\ne0\\Leftrightarrow$ a directed routing cycle. · **↑** resolve the self-probe-current crux (the protected current is the routing-cycle affinity, not the arrival rate) \+ a spec on a 3-station cyclic network. · **✗** a non-reversible cyclic queueing NESS where the two frames agree despite affinity $\\ne0$, or disagree with no routing cycle. · **→** the two-frame construction (new projection) \+ the queueing adoption row.  
* `epsilon-hub-transport` **\[steeping\]** — *verdict:* a transport law on the distance-to-marginal-point $\\varepsilon$ — one $\\varepsilon$ in four registers (aging $\\beta\\approx1-\\varepsilon$, heat-tax $\\alpha(\\varepsilon)$, branching $\\to1$, critical length); degenerate at both endpoints, content in the interior. · **↑** a runnable collapse spec measuring $\\ge2$ registers and inverting to a common $\\varepsilon$. · **✗** the maps are mutually inconsistent, or two registers are tied definitionally. · **→** the memory-exponent and SOC falsifiers; coarse-graining.  
* `affinity-hub-transport` **\[steeping\]** — *verdict:* a transport law on the affinity $a$ — one $a$ in three registers (queue load $-\\ln\\rho$, relaxation-oscillation $Q(a)$, phase-lock suppression); degenerate at both ends, signal in the mid-band. · **↑** a runnable collapse spec inverting $Q$ and phase-lock suppression to a common $a$. · **✗** the two inferred $a$'s cannot agree by construction. · **→** the queueing adoption row \+ the relaxation-oscillation and phase-locking claims.  
* `predictor-soft-sector-blindness` **\[steeping\]** — *verdict:* an output-trained (feedforward) predictor occupies the **neither corner** by architecture — large soft-sector capability, hard sector structurally absent (acyclic adjacency ⇒ $\\mathcal{A}\\equiv0$). On a hard-sector question it is biased **confident-and-false**: a narrow-but-wrong confidence interval is the metric-sector signature of *averaging across* a topological branch boundary the predictor cannot represent — distinct from contingent under-sampling, which more data fixes. (Surfaced from the AlphaFold / prediction-powered-inference case — Angelopoulos–Bates–Jordan: 200M predicted protein structures give tight-but-wrong CIs on a phosphorylation×fluctuation test; phosphorylation is a driven NESS cycle — `pa:kinetic-proofreading`.) · **↑** a substrate where soft/hard cases are labelable: the bias **concentrates on hard-sector cases**, does **not** shrink with soft-sector data, and boundary-straddling ground truth (anchors at the separatrix) restores coverage with fewer labels than uniform anchoring. · **✗** the bias is uniform or shrinks with any added data — then it is plain under-sampling and character adds nothing. · **→** `character.md` §The two-survivals plane (the neither corner; soft-sector capability silent on the hard sector). Same shape as `nudge:loaded-endpoint-descent` — apparent capability ≠ the protected observable.

### Nudges — steering candidates (a parallel doc-gate track; HELD)

A **nudge** corrects how character *steers a reader* — a sentence the core is missing or mis-places, diagnosed from a misread — not a claim about a substrate. It does **not** climb the world-rungs above (no substrate, no falsifier-instance); its path is two-rung: `candidate → (recurrence: a 2nd independent misread trips the same wire) → core`. The `↑` is recurrence; the `✗` is "the existing core text already steers correctly — the miss was reader sloppiness." Each entry additionally logs its *trigger(s)* (the misreads \= its support) and the *shape* it instances (the steering-failure class).

**Policy — HELD.** No nudge promotes to core yet, even one that already looks recurrence-mature. Accumulate the corpus and read where the shapes cluster *first*; the holding pen exists to see the shape before committing a sentence. Promotion is a deliberate later event, never automatic on the gate.

* `nudge:loaded-endpoint-descent` **\[nudge\]** — *verdict:* a loaded macro-phenomenon (sleep, a market, a mood) is read by **descent** to its substrate-general skeleton, never directly — `ℭ` is degenerate at loaded endpoints exactly as at the boundary faces ${0,1,\\infty}$. Two steering misses. **(A) Periodicity is not the hard bit** — an observable oscillation establishes neither a protected current nor a minted bit; only the *protection test* does (noise-independent $\\mathcal{A}$; a sign that reset-and-re-drive shows is wiring-set or re-rolled). The §Composition gate ("the spectral count is circular; the non-circular observable is the protection test") sits *downstream* of the minting language instead of guarding it, so a reader walks past it. **(B) Decompose before composing** — a switch is **branch-only**, an oscillator at most **current-only**; their co-occurrence is two corners, not a `both`, until the union graph's frustration is exhibited and the composite's current *measured*. · **↑** recurrence — a 2nd loaded-endpoint misread tripping the same wire (then a HELD-policy review, not auto-promotion). · **✗** the existing §Composition gate already steers correctly and the miss was reader sloppiness (then drop). · **→** `character.md` §Composition (the protection test), §The two-survivals plane (locate the corner before any composite claim), the open-interval/degeneracy discipline. · *triggers:* the outside-model sleep essay (2026-06-03) — read sleep as a minted `both`; **(2026-06-13) archetypes-as-circulations** — six loaded macro-functions (persist / replicate / acquire / defend / coordinate / explore) read as hard bits when only the protection test certifies one; **(2026-06-13) sink-does-all-the-work** — the *dual* over-read, a loaded endpoint (the sink) credited with the whole dynamics. · *shape:* **apparent-structure ≠ the protected observable** (only the protection test certifies the hard bit) — now elevated to the cross-track class `shape:apparent-structure-not-observable` (see Shapes). Already recurring in-core (logged, NOT promoted): the neither-corner corollary ("soft-sector capability is silent on the hard sector"), the §Composition circularity gate, and this nudge; cf. the deformer $\\lambda{=}0$ reciprocal case ($\\mathcal{A}{=}0$) and the detailed-balanced DNA cycle ($\\mathcal{A}{\\approx}0$ pre-fuel). **Recurrence FIRED** (2026-06-13, two further in-shape misreads) → the `↑` gate's HELD-policy review is now **due**; the dual over-read sharpens the shape from "don't over-credit the figure" to **don't over-credit either pole — the gradient is the bit**.

### Shapes — cross-track recurrence classes *(navigation; nothing moved or deleted)*

A **shape** names a steering-failure (or structural) pattern that recurs across *different rungs/tracks* — the nudge holding pen, the steeping register, in-core gates — so the recurrence is legible in one place. A shape does not itself promote; it triggers the relevant track's review (e.g. a nudge's HELD review) once its instances cluster. Like the Dashboard, this is a navigation layer: every referenced entry is preserved verbatim in its own rung.

* `shape:apparent-structure-not-observable` — *apparent structure (periodicity, capability, a striking figure, a deep-looking endpoint) is never the protected bit; only the protection test certifies the hard sector.* **Dual form (2026-06-13):** don't over-credit *either* pole — neither the figure nor the sink; the **gradient** carries the bit (a sink read as doing "all the work" is the same error as a figure read as self-sustaining). *Instances:* `nudge:loaded-endpoint-descent` (sleep as a minted `both`); `predictor-soft-sector-blindness` (feedforward predictor confident-and-false on hard-sector questions); in-core neither-corner corollary (soft capability silent on the hard sector); in-core §Composition circularity gate (the protection test is the non-circular observable); **(2026-06-13)** archetypes-as-circulations (six loaded macro-functions read as hard bits); **(2026-06-13)** sink-does-all-the-work (the dual — a loaded endpoint credited with all dynamics, now its own entry `sink-extracts-the-work` with the falsifiable, non-over-read form). *Status:* recurrence fired; `nudge:loaded-endpoint-descent`'s HELD review is **due** — name the sentence, **do not auto-promote**.

### Sharpening — active work on a named owed item

* `seam-classification` **\[sharpening; layering crossed\]** — *verdict:* every deformation-calculus threshold is a **seam** read through singularity theory (`pa:singularity-stratification` — Thom/Arnold/Golubitsky–Schaeffer/Whitney): a (singular set, **codimension**, universal unfolding), so the rounding exponent and "hard vs soft" *are* the normal form, not separate results. The instances: the composition **associator** is the **codim-0 smooth bulk** (analytic series, off the singular set); the **hold-both/coexistence threshold** is a **codim-1 transcritical** the drive unfolds to a smooth crossover ($q{=}\\tfrac12$; an SSB pitchfork rounds at $\\tfrac13,\\tfrac23$); the **Boolean-deviation** is a **codim-1 crossover** of width $w(D)$ (forced by a substrate FDR — `battery:seam-collapse`); the achiral $\\mathcal{A}{=}0$ point is the **singular set itself** (read the invariant only in the codim-0 interior); the **exceptional point** is a **codim-2** singularity in the shear sector; the amplitude/topological **face-boundary** is the one unclassified seam — a smooth codim-1 unfolding (faces independent) or a topological seam (faces correspond), complex pair $\\Leftrightarrow\\mathcal{A}\\ne0$ at eigenvalue coalescence. **Crossed:** the **metric→singularity→topology** layering — the singularity layer fixes which metric quantities vanish smoothly vs across a seam, and the **protected sign is the non-unfoldable stratum** (`character.md` §The two tangent sectors). **Discipline (`affordance`):** the rigorous unfolding/codimension core, not the catastrophe overreach — a normal form only where forced. · **↑** codimension *read off a real substrate* (a seam whose metric observable vanishes smoothly while its topological partner cannot unfold) → codimension a core primitive. · **✗** a seam with no well-defined codimension; or a protected sign that unfolds smoothly with $M$ stable and $\\mathcal{A}\\ne0$ — a propagating kill, attempted and survived (`experiments/kill_protected_sign.py`). · **→** `character.md` §The two tangent sectors; receipts §Deformation calculus.  
* `dimensionless-substrate` **\[sharpening\]** — *verdict:* the dimensionless self-probe instanced on rock-paper-scissors; the residual is a *second* real substrate to promote the self-probe ruler. · **↑** a second real substrate hosting the dimensionless self-probe at the standard operating points → `battery`. · **✗** the dimensionless self-probe fails to close on a real frustrated substrate. · **→** the self-probe ruler; ties `battery:dimensionless-self-probe`.  
* `consolidation-ascent` **\[sharpening\]** — *verdict:* one heat-tax mechanism, three faces — an *endogenous* offline drive-withdrawal duty cycle, a compression operator contending with active maintenance for one shared server, and the tower ascent that *is* consolidation. The topological bit threads the duty cycle by a **definite mechanism**: only the **external-rewiring** branch survives a drive-stop ("restart returns the same circulation"), so consolidation is the **conversion of thermalized-crossing traces (lost on reset) into structurally-stored ones** before the drive withdraws — $\\mathcal{A}$'s *value* persists in the wiring while its *reality* (the current) and the soft/amplitude sector relax. The off-phase is survivable **only at $\\beta\<1$**: the slow tail clears the gap; $\\beta{=}1$ relaxes to the floor and re-mints. The **gap statistics (burstiness) are the queue-tail face of the same $\\beta$** as the aging exponent and the kernel — so the duty cycle is **locked to the $\\beta$-collapse, not a free knob**. In the limit, continuous drive is the loaded endpoint and the object is a **cyclic character** (a limit cycle through `ℭ`, not a fixed point) — `flow-resident-number` territory. · *candidate substrate (2026-06-13):* a **sleeping nervous system** — hierarchical, $\\beta\<1$, an endogenous duty cycle, measurable gap/aging/tail statistics. · **↑** a runnable spec on that substrate: the coarse-grained kernel form measured forced-not-fitted **and** gap-burstiness, aging, and queue tail inverting to **one $\\beta$** → `battery` (the sharpest falsifier finally given a substrate). · **✗** the coarse-grained level fails the two-mode-kernel form (kills RG-closure for $\\beta\<1$); **or** a duty-cycler whose persistent bit is **thermalized-crossing yet survives the reset** (breaks the value/reality split); **or** the three $\\beta$-registers fail to collapse. · **→** receipts §RG closure (the $\\beta\<1$ open item); `staked:memory-collapse` (shared substrate); `flow-resident-number` (the cyclic character); `character.md` §Branch membership (the two flip-modes as the consolidation mechanism). *Enriched 2026-06-13 (sleep / burstiness / duty-cycle session).*  
* `frustration-ascent` **\[sharpening\]** — *verdict:* the generative-recursion bet, resolved **conditional** — *generative* of organization/chirality/topology (these flow with the NESS affinity, so they are mintable — legal) and *parasitic* on the drive (continuous-amplitude autonomy is supplied, not minted — receipts §Amplitude autonomy, a closed negative). The single-substrate triple obstruction (self-light \+ gapped \+ complex-pair-seedable mutually frustrate) is a **same-level** closure obstruction, **evaded by a two-level stratified cascade**: a base self-lights and selects a handedness, a gapped upper carries an autonomous protected cycle that survives drive removal. The legal three components (minting / protection / sustained-as-run-loop) were shown end-to-end (calibration) and confirmed on the DNA reaction network (composite branch, crossed). · **↑** a *real* substrate where a coarse register's chirality mints **and** the cascade closes robustly (the only legal mintable). · *candidate instrument (2026-06-13):* **JCVI minimal cells** — genome reduction is a physical probe of *which circulations are structurally necessary vs historically contingent* (a module independently toggleable off, then restored, is a minted protected circulation — the same toggle as fuel-driven minting), a route to the real-substrate chirality-minting instance this entry owes. · **✗** amplitude-autonomy minting — **closed negative** by necessity. · **→** the closure / composition; stays sharpening for the remaining real-substrate chirality-minting joint instance \+ full end-to-end robustness.  
* `self-referential-closure` **\[sharpening\]** — *verdict:* the open item §Status names ("a `⊗`-fixed-point among its own drivers") unpacks into a **three-layer operation stack**, of which the core has only the first. **(i) Minting** — coupling creates a protected bit neither part carried (HAVE: §The minting claim; four real instances). **(ii) Templating** *(the question asked: can a protected circulation bias sign(𝒜) of the bits it mints, through its own recursive closure, toward its own value?)* — this is **value-transmission**, categorically distinct from minting's **existence**: signed-graph theory supports minting *precisely because* it creates a bit **neither parent carried**, whereas heredity needs the **parent's specific sign** propagated — so the framework has strong support for minting and **nothing** for templating. The two flip-modes already gate it — a structurally-stored branch survives a reset (primitive heredity-via-fixed-geometry), a thermalized-crossing one is re-rolled — so templating *is* `consolidation-ascent`'s thermalized→structural conversion read as **inheritance**, and "RNA" is its **portability** upgrade (a branch store that travels with the loop instead of being pinned in the reservoir geometry — `drive-not-primitive`). **(iii) Self-wiring** *(the under-collapsed bigger layer)* — every minting result takes the **coupling graph as exogenous** (§Composition, §The minting claim hand you A, B, and the union edges; Harary balance is run on a *given* union). In the recursive closure the minted characters **re-enter the input set as new nodes/edges**, so the system **writes its own union graph** — minting goes *endogenous*. This is "a structure becomes its own environment / creates its own physical geometry" formalized, and it is **not** the conjugate cascade (that is the *cost* ledger, σ_hid≥0; this is the *construction*). It is exactly the **non-prestatable / adjacent-possible** regime §The morphospace flags but does not build ("the space is generated as the system moves"), and it is the autopoietic core the north star points at — *the loop produces the wiring that runs the loop*. **Heredity = templating *over* self-wiring**: transmitting branch values across a graph the system is simultaneously rewriting — that compound, not either piece, is what "alive vs merely-dissipative" turns on, and why a wind-tunnel vortex (flame-class, b₁=1, fixed exogenous graph) is **not** alive. **Thermodynamic guard:** self-wiring is **not** self-driving — the free energy stays external (`sink-extracts-the-work`, `drive-not-primitive`: a source↔sink dipole is non-negotiable, you cannot re-drive on your own exhaust); the recursion generates the **coupling topology**, never the drive. **Imports check (2026-06-19, per the no-synthetic-novelty discipline):** both layers are **over-determined by established math**, so the run is *calibration* not discovery — templating by `multitype-branching` (offspring-sign = the mean-matrix assortativity, Perron–Frobenius asymptotics), `frank-autocatalysis` (chiral amplification = handedness templating, *already imported*), and `quasispecies` (the error threshold = templating failure = the ✗); self-wiring by `raf-sets` (self-producing closure with a sharp existence threshold), `hypercycle` (a directed cycle that reproduces itself), and `akr-balance-dynamics` (signed-graph rewiring). **Residue to watch — the one place novelty could sneak in:** AKR self-wiring relaxes toward *balance* ($\mathcal{A}\to0$, dead), so sustained frustration is the **drive re-injecting** what the graph-dynamics removes (`drive-not-primitive`) — a composition of imports; verify the run rides this and is not proving "frustration propagates" from scratch. **Bootstrap vs advanced (2026-06-19, Ron's prioritization):** the imports above are all **downstream** — they assume discrete replicating/catalyzing *types* (a copyable tape already exists). The **bootstrap** — reproduction-with-heredity on a *pre-chemical, physics-only* substrate — is grounded by `von-neumann-automata` (the necessity: open-ended heredity needs a *copyable description*; a fixed-geometry tape is sub-threshold) and `cairns-smith` (the candidate: geometry/mineral as the first tape — *unproven*). Both demand the bit be **copyable**; a wind-tunnel vortex / coastline gives self-maintenance + degenerate pattern-replication but **no copyable description**, so naive "life from geometry" is sub-von-Neumann-threshold. **The bootstrap is open in the literature — not solved here — so the framework claims none of it** (claiming it would be the synthetic novelty this pass guards against). **Priority consequence: templating *is* the bootstrap test** (can a physical loop copy its own sign(𝒜)?), not an advanced feature — it stays the focus; **self-wiring + the cascade-depth law are advanced, deprioritized until the bootstrap clears.** The pre-chemical-tape question itself is `steeping` (open, no falsifier); only the templating *reading* (given a kernel → multitype asymptotics) is sharpening. **Templating MEASURED (2026-06-19):** the run below located the threshold — the kernel is real, faithful, drive-independent **but degenerate** (`C≈1 bit ≪ k`); physics buys self-maintenance + ~1 bit of degenerate templating, and open-ended heredity stays sub-von-Neumann-threshold without a copyable tape (so the bootstrap is *calibrated*, not eased). **Self-wiring + the cascade-depth law remain owed.** · **↑** *(templating → battery — **FIRED 2026-06-19**, `experiments/templating_kernel.py`)* the synthetic recursive-`⊗` ran: a seed protected circulation, iterated through its own closure, **biases sign(𝒜) of newly minted circulations toward the seed's** above chance (kernel `P(same)=0.89`, **parity-equivariant to machine precision**) and **drive-independently** (offspring sign invariant across the drive `D`, flips only on rewiring the seed), with the `κ=0` random-sign control failing to bias (exact 50/50). The non-trivial **von Neumann discriminator** is the headline: `I(s_parent;s_child)` **saturates** at `C=½log₂(1+κ²/σ_b²)=1.0 bit ≪ k` (rank-1 shared field; `I/k→0`), against the tape reference `I=k` and the field-OFF null `I=0` — so the templating is **degenerate** (field/crystal-class, calibration measuring where the von Neumann threshold sits + feeding `multitype-branching` asymptotics), **not** open-ended; lineage `⟨s₀sₙ⟩=(2p−1)ⁿ` decays (no Pólya lock-in to the seed). Receipts §Composition · *Templating — the offspring-sign kernel is degenerate*. *(self-wiring → sharpening)* an explicit operation $G_{n+1}=F(G_n,\text{minted}_n)$ producing a frustrated union graph from prior products, instanced once synthetically. · **✗** *(templating)* the recursion mints with sign(𝒜) **independent of the seed** (offspring handedness re-rolled each closure) — then minting is all there is, there is **no pre-genetic heredity**, abiogenesis is **re-described not eased**, and "reproduction precedes life" loses its mechanism; *(self-wiring)* the endogenous graph reduces to a fixed exogenous one (re-entry adds no edges that change frustration) — then the closure is ordinary cascade, not autopoietic. · **→** `character.md` §Cascade / the self-referential limit (the open item), §The minting claim, §The morphospace (the non-prestatable regime); the parked `project_syn3_alive_loop_northstar` (loop-of-the-loop = self-wiring; alive ≠ reproduction). **Sibling of** `frustration-ascent` (this is its heredity+autopoiesis decomposition), `consolidation-ascent` (thermalized→structural = the templating substrate), `selective-coupling-class` (the depth law $N(\mathcal{S})$ bounds how far self-wiring extends before $\varepsilon\to1$). *Raised 2026-06-19 (the abiogenesis / templating-vs-minting exchange — templating is the entry asked for, self-wiring the layer the first pass under-collapsed).* **Build LANDED (2026-06-19):** `experiments/templating_kernel.py` (+ `templating_kernel.png`, three panels: kernel heatmaps · the `I(k)` saturation curve · the lineage decay) ran the pre-registered test in `docs/handoff_templating_synthetic.md` and fired **outcome (b) — degenerate templating** on all six gates; the non-trivial **von Neumann discriminator** `I(parent string; child string)` vs `k` **saturated** (`C≈1 bit ≪ k`, `I/k→0`), so physics sits on the *degenerate/field-class* side of the threshold, not the *tape-class* side — the framework's "not off the ground without a tape" is calibrated. Receipts §Composition · *Templating*. Below the line (synthetic = calibration); **no `character.md` edit.**  
* `wall-as-type-boundary` **\[sharpening\]** — *verdict:* the marginal point is a type-boundary **ladder** of plateaus (normally hyperbolic invariant manifolds carrying the universal normal form) connected by three boundary kinds — ignition (the constructive landing), topological flip (the free sign-face transition), and closure-loss ($\\varepsilon\\to1$, loss of normal hyperbolicity); only the last is wall-like. The plateau side is instanced on the coupled Stuart–Landau cascade (CLV minimum angle bounded), and the closure-loss side via a Benjamin–Feir route ($\\theta\_{\\min}\\to0$, $\\lambda\_{\\max}\>0$); the $N{=}3$ cascade reaches the boundary *without* chaosing — instancing *loss-of-hyperbolicity ≠ forced chaos*. · **↑** on a real cascade: levels read as plateaus, transitions as the named boundaries, a level at $\\varepsilon\\to1$ shows $\\theta\_{\\min}\\to0$ → promotes the ladder. · **✗** the CLV discriminator fails to separate plateau from boundary, or a real cascade level is not such a plateau. · **→** coarse-graining (the marginal point as loss of normal hyperbolicity); receipts §Marginal point. The canonical delay-driven mechanism and both sides on a faithful real cascade are still owed.  
* `chirality-protection` **\[sharpening\]** — *verdict:* the protected object in a minted circulation is the gauge-irremovable affinity / discrete graph-flux sign — **not** the exceptional pair (suppressible) and **not** an integer charge (holonomy sub-integer); run synthetically (sign 0/200 graph-fixed deformations, exceptional pair killed 53/200, holonomy $\\le0.16$); the binding discharged by import. Below the line because synthetic \= calibration. · **↑** a *second, real-substrate* instance of the affinity/exceptional-pair separation. · **✗** a graph-fixed smooth deformation reversing the sign without rewiring; or a substrate where overdamping the exceptional pair also erases the affinity. · **→** receipts §Chirality protection, §Topological-drain; `battery:sign-interior`.  
* `chiral-rotor-triad` **\[sharpening\]** — *verdict:* a candidate **mechanical** substrate for the protected-sign claim — three identical chiral rotors (a spherical-harmonic chiral shape, $\\chi=\\varepsilon\_2\\varepsilon\_3\\sin\\delta$) coupled in a driven ring (a mechanical **May–Leonard** / cyclic-dominance triad), the circulation **sign locked to the shape chirality**, drive-invariant ($\\Omega$ scales magnitude; $J\\to0$ as $\\Omega\\to0$), flipped only by rewiring the shape ($\\delta\\to-\\delta$). The non-reciprocity is **effective, not bare-mobility** — the instantaneous Stokes mobility is reciprocal (Lorentz); the drive \+ phase-reduction generate the antisymmetric coupling, whose **non-vanishing is forced by a selection rule** (the chiral propeller coupling $\\propto\\langle Y\_2\\Vert\\mathbf V\\Vert Y\_3\\rangle$, nonzero by **Clebsch–Gordan $2\\otimes3\\supset1$** — the $l{=}1$ vector sector *is* the translation–rotation sector), activated by the ring's $\\Delta\\varphi=2\\pi/3$ orientation (the $\\Delta\\varphi=0,\\pi$ mirror kills it). Derivation-closed (three independent external passes, adjudicated; the *Triskele* line), **unrun → synthetic \= calibration**; its real value is a **route to the *physical* second instance** `chirality-protection` owes. · **↑** the propeller coefficient $C$ computed (a Stokes boundary integral — nonzero guaranteed) \+ the drive-sweep / chirality-flip run synthetically → `battery`; a **built physical** chiral-rotor triad → feeds `chirality-protection`'s real-substrate gate. · **✗** $C=0$ (the coupling vanishes despite the selection rule), or the circulation sign tracks the drive direction (not parity-locked), or the shape carries a residual improper symmetry (not chiral). · **→** `chirality-protection`, `battery:sign-interior`; ties the crossed *deformation chart · homochirality*. *Derivation closed 2026-06-10 (external-model derivation \+ adjudication).*  
* `cascade-ledger-split` **\[sharpening\]** — *verdict:* the conjugate cascade's two ledgers are one coarse-graining EP decomposition — $\\langle\\sigma\\rangle\_{\\text{tot}}=\\langle\\sigma\\rangle\_{\\text{res}}+\\langle\\sigma\\rangle\_{\\text{hid}}$, the hidden part the integrated-out fast circulation's dissipation, aligned with the metric/protected split exactly by Schur (`pa:transverse-decomposition`); closes by composing standing imports (heat-tax tower \+ transverse decomposition \+ `pa:esposito-coarse-graining`/`pa:timescale-ep`), analytical-only. · **↑** a real two-level substrate measuring $\\langle\\sigma\\rangle\_{\\text{res}}$, $\\langle\\sigma\\rangle\_{\\text{hid}}$, $\\langle\\sigma\\rangle\_{\\text{tot}}$ — they sum, and the hidden part sits in the protected sector → `battery`. · **✗** a two-level substrate where the three fail to sum, or the hidden EP sits in the metric sector. · **→** core §The conjugate cascade; receipts §Conjugate-cascade ledger.  
* `qec-transverse-decomposition` **\[sharpening\]** — *verdict:* the surface-code reading of the transverse decomposition, **computed** (was asserted; Open Thread \#2 resolved). In the verified $\[\[9,1,3\]\]$ / $\[\[25,1,5\]\]$ rotated surface code the logical sector carries zero syndrome (symplectically transverse to the syndrome sector), the protected bit is **written at unit cost and flipped only at the code distance** ($P\_L\\propto p^{(d+1)/2}$, enumerated), and the bit-flip barrier exponent is **exactly invariant** to a syndrome-active phase (Z) current — CSS X/Z independence *is* the transverse separation — leaking $\\propto\\delta$ only under Y-mixing. **Boundary mapped:** a *same-sector* current is never exactly transverse (every detectable error lies on a min-weight logical string), so exact $\\Delta V\\perp\\mathcal{A}$ is **symmetry-protected, not generic** — the same boundary as the triad's $Z\_3$ (`gmam_symmetry_break_probe`). Below the line because synthetic \= calibration; but it unifies the three checked substrates (triad, surface code, cascade) under one mechanism (Schur), and carries the seashell's re-homed deposit. **Now a portable protocol** (the transverse-decomposition test, handoff §TDT), run on a third metric observable — the **aging exponent** (biased ring): aging is invariant to the protected current to machine precision across a wide affinity sweep, coupling on only when the ring's $Z\_N$ symmetry breaks (receipts §Glass aging transverse) — so the current is barred from *all* metric-sector observables, not just barriers; the cross-rule "aging never couples to circulation" computed. **Scope (transferable):** the selection-rule *threshold* — exactly zero coupling iff the protecting symmetry holds — is the substrate-invariant; the *onset power* is not (QEC linear in δ, glass ∝δ²). · **↑** a *real* substrate showing barrier-invariance to a symmetry-separated current plus the leak under mixing → `battery`. · **✗** a CSS code whose logical-barrier exponent shifts under a pure conjugate-sector current; or a within-sector current shown exactly transverse with no protecting symmetry. · **→** `character.md` §The two tangent sectors \+ §The two-survivals plane; receipts §QEC transverse decomposition; `pa:transverse-decomposition`. *Raised \+ computed 2026-06-05.*  
* `selective-coupling-class` **\[sharpening\]** — *verdict:* a coupling **class** distinct from the two the core names — metric (averaging / competition / EP-coalescence) and frustrated (minting) — **selective coupling**: hard-sector-rich, soft-sector-poor, passing circulation while suppressing population mixing. The substrate-general property is *selectivity*, not fluids — a coordinate where mixing is expensive and one where circulation is cheap ($\tau_{\text{mix}}\gg\tau_{\text{circ}}$). **Two tiers, not to be conflated:** **Tier 1 — symmetry-exact** ($V_{AB}=0$ by a symmetry: the DFS / Schur / transverse-decomposition split — *already core*, §The two tangent sectors; nothing new) and **Tier 2 — smallness-suppressed** ($V_{AB}\ne0$ but $|V_{AB}|\ll|\Gamma_{AB}|$, a *regime* not a symmetry — the new content, exactly the form I5 / 'by symmetry not by smallness' forbids letting masquerade as protection). Coined: the **stratification number** $\mathcal{S}\equiv|\Gamma_{AB}|/|V_{AB}|$ (Richardson-like; the fluid's one genuine loan), $\mathcal{S}\gg1$ stratified, $\mathcal{S}\to\infty$ recovering Tier 1. Realizations span the tiers: symmetry / gauge / parity sectors (Tier 1); density, **timescale** (\= the framework's own slow-manifold $\varepsilon$, fast\=topological / slow\=metric — a *reframe* of existing machinery, near-circular if offered as the convergence *mechanism*), chemical-membrane (Tier 2, the barrier itself driven) — **a modular graph does *not* qualify** (a sparse inter-module cut suppresses *both* channels; selectivity needs skew / non-reciprocal links, which is the union-cycle condition, not modularity). The **forced** sub-results promoted to core 2026-06-18: the minting holonomy $\mathcal{A}_{\text{union}}=\oint(l_A+l_B+\Gamma_{AB})\cdot dx$, exactly $\oint\Gamma_{AB}\cdot dx$ when the parts are non-circulating; the skew $\Gamma$ ($\rho^\top\Gamma\rho=0$) as the no-metric-work **conduit** (the puppet of `sink-extracts-the-work`), **necessary but not sufficient** ($\dot x=J\nabla H$ is skew yet $\mathcal{A}=0$); minting **stratum-agnostic** while the metric proximity laws need a common stratum (core §The minting claim, §The space of characters, §Motion and proximity; receipts §Composition). The un-forced **payload** \= a **cascade-depth law**: the Tier-2 leak ($V_{AB}\ne0$) compounds level-to-level and eventually drives $\varepsilon\to1$, so depth-before-collapse $N_{\text{levels}}\sim f(\mathcal{S})$ — the quantitative form of 'persists farther from the collapse boundary', and the one thing Tier 2 buys over Tier 1. **Theorem-form (the earnable object, the anthropic wrapper stripped):** define the structure-production ratio $M=$ (minted protected structure)/(coalesced lower-level structure); a cascade is indefinitely extensible ($N\to\infty$, $\varepsilon<1$ sustained) iff $M>1$, with $N(\mathcal{S})$ diverging as $M\to1^+$ — and $M=1$ plausibly *is* the marginal point $\varepsilon\to1$ (minting failing to outpace coalescence \= the slow manifold failing to persist), unifying $M$, $\mathcal{S}$, and $\varepsilon$. **Observer corollary, $\beta$-tightened:** an observer needs not *much* predictive information but *long-memory* ($\beta<1$) predictive information — and $\beta$ is generated only by the cascade ($\beta\approx1-\varepsilon$, `staked:memory-collapse`), so observer ⇒ $\beta<1$ ⇒ deep cascade ⇒ sustained $\varepsilon<1$ ⇒ ascendant couplings; every arrow up to the last is a framework result. **The last arrow (necessity) is unearned** — and the alternatives sort, not list: timescale-separation / symmetry-protection / error-correction are *instances* of ascendant coupling (not competitors), modularity / sparsity are insufficient (non-selective cuts), so the one genuine open competitor is **active maintenance** (evolution / adaptive rewiring / error-correcting feedback — hierarchy held by *repair*, not coupling geometry). Non-circularity guard: $\mathcal{S}$/$M$ must be measured from the coupling alone, then $N$ predicted, then checked (the ↑ gate). The anthropic reading (observers exist only where the substrate admits ascendant couplings) is a **parked implication** — distinct from the alive-loop north star — not a work-item. · **↑** a real or minimal-generated substrate with tunable $\mathcal{S}$ ($\ge3$ strata) showing (i) cross-stratum minting localized in $\oint\Gamma\cdot dx$ with the sign drive-locked (flips on rewiring, not on drive), (ii) the metric channel parametrically suppressed (not zero), (iii) $N_{\text{levels}}(\mathcal{S})$ measured forced-not-fitted → `battery`. · **✗** (a) no selectivity outside Tier-1 symmetry — every substrate that suppresses $V_{AB}$ suppresses $\Gamma_{AB}$ equally; (b) collapse depth independent of $\mathcal{S}$ (Tier 2 adds nothing over Tier 1); (c) a stratified stack whose $\sigma_{\text{hidden}}$ sits in the *metric* sector — fires `cascade-ledger-split`'s kill (the inter-layer dissipation must track $\operatorname{sign}(\mathcal{A})$, else it is plain viscous loss). · *caution:* the stratified-vortex narrative was a fresh instance of `shape:apparent-structure-not-observable` — co-occurring spin \+ confinement read as a minted `both`; a 2-body co-axial *lock* is the $N\=2$ gauge-removable case (metric entrainment), **not** minting, and any 'Both'-corner placement needs its two symmetry-related branches \+ saddle *named*, not asserted. · **→** `character.md` §The space of characters, §Composition / §The minting claim, §The two tangent sectors, §Coarse-graining and the marginal point. **Absorbs** the retired `cross-stratum-transduction` (topological transduced faithfully while the metric carrier is fungible — its deposit "a sign propagated laterally for free, so topology survives the tower"; now to be read on a *live, driven* substrate, the **shell** carrier having dead-ended as a frozen projection — verdict in `character_substrate_ledger.md`, not a tombstone: a weak pass, not an overturned claim); **sibling of** `sink-extracts-the-work` and `cascade-ledger-split`; **distinct from** `cascade-climbing-universality` (a KPZ growth-fluctuation law, not a depth law). *Raised 2026-06-18 (the cross-stratum marathon sessions \+ outbound-channel integration and 3-round critique; abstracted from stratified fluids to the selective-coupling property).*  
* `curvature-as-coupling-bias` **\[sharpening\]** — *verdict:* local geometry — the quasipotential-saddle Hessian (escape prefactor), the spectral-sheet curvature (exceptional-point onset), or the leading symmetry-allowed $O(\\mathcal{A}^2)$ response coefficient — **modulates** how strongly already-connected sectors communicate (biasing branch occupancy and transition rates) but does **not** generate the protected circulation (existence \= global frustrated topology, protection \= cycle affinity). The surviving second-order coefficient is *measured* (the driven colloid — receipts §Colloid ring transverse) but its status as a **geometric** curvature (a Hessian / reduced-manifold curvature) rather than **merely the leading analytic coefficient** of the response is unestablished — the one open leap. · **↑** a forced-not-fitted derivation identifying the surviving $O(\\mathcal{A}^2)$ coefficient with a geometric curvature *universally* (not case-by-case), or a second real-substrate instance of the same geometric identity → `battery`. · **✗** a substrate whose surviving second-order coefficient carries no curvature interpretation (purely analytic) — then "curvature" is dropped and the claim reduces to its safe form ("the leading allowed coupling is a second-order response coefficient"). · **→** `character.md` §Motion and proximity (the cross-rule, "Local geometry, bounded") \+ §The two-survivals plane; receipts §Colloid ring transverse, §Branch-survival barrier; `pa:transverse-decomposition`. *Raised 2026-06-07* (from the gallery's saddle-susceptibility reading \+ an outside-channel curvature analysis). **Caveat logged:** the gallery's connectedness-boundary "saddle" is a *parameter-space* bifurcation, distinct from the *state-space* quasipotential saddle — both exhibit criticality → high susceptibility, but they are not the same object; do not bridge them silently.  
* `cascade-climbing-universality` **\[sharpening\]** — *verdict:* the bet that **cascade-climbing has a universality class** — that "the tower ascent that *is* consolidation" is a **stochastic growth interface**, its level-to-level roughening governed by universal exponents and a universal fluctuation distribution, not substrate-specific detail. The candidate (an *import*, conjectured to apply) is **KPZ**: the lateral propagation `selective-coupling-class` records ("the hand propagated laterally for free") is the candidate KPZ nonlinearity $(\\lambda/2)(\\nabla h)^2$, and the self-similar coiling it reads as an **RG fixed point** is the candidate KPZ scaling. The discriminator is **driven vs relaxational at the scaling level**: KPZ ($\\beta\_{\\text{KPZ}}{=}1/3$, $z{=}3/2$ in 1+1D; **Tracy–Widom** height fluctuations) is the *driven* growth class; Edwards–Wilkinson ($\\beta{=}1/4$, $z{=}2$; **Gaussian**) is the *equilibrium* one — so the conjectured bridge is **KPZ-class $\\Leftrightarrow$ the cascade carries a protected current ($\\mathcal{A}\\ne0$)**, EW-class $\\Leftrightarrow\\mathcal{A}{=}0$ (the fluctuation distribution, not the dimension-dependent exponent, is the robust readout). If it holds, the **growth statistics are a soft-sector readout of the hard-sector affinity** — the same metric$\\perp$topological logic as the TDT (`qec-transverse-decomposition`), now at the level of a scaling class. · **↑** a real cascade read as a height field: the fluctuations land in **one** class **and** the class **tracks $\\mathcal{A}$** (KPZ/Tracy–Widom when the cascade is driven, EW/Gaussian when detailed-balanced) → `battery`. · **✗** the interface shows mean-field/Gaussian fluctuations or exponents in **no** known class (climbing is substrate-specific, no universal scaling); **or** a driven cascade ($\\mathcal{A}\\ne0$) robustly **EW-class**, or a detailed-balanced one **KPZ** (breaks the bridge); **or** the height-field reading is not faithful (the cascade is not an interface). · **→** `selective-coupling-class` (the growth cascade, the RG fixed point, the lateral-propagation nonlinearity), `consolidation-ascent` (the tower ascent), `wall-as-type-boundary` / `seam-classification` (the thm9 crossover / scaling machinery); `character.md` §Coarse-graining and the marginal point; import flagged for `character_prior_art.md` (KPZ / EW / TASEP). *Raised 2026-06-13 (the session's "universal cascade-climbing structure" thread — the lead the session marked as its most important.)*  
* `sink-extracts-the-work` **\[sharpening\]** — *verdict:* the source/sink/conduit decomposition read with the **sink as first-class, co-equal with topology**. The protected topology is **paid upfront** (minted at cost, then inert — a structurally-stored branch at zero marginal upkeep, "a sign is free to maintain"); the **conduit** between is a fungible metric carrier that **commits nothing** of its own (the puppet); the **work is extracted at the sink** — the cold / low-entropy reservoir is what turns stored topology into a running circulation, and $a{=}\\ln(G\_0/L)$ is a **two-sided ratio**, so the *gradient*, not either pole, does the work. **(Refined 2026-06-13 — `drive-not-primitive`:** 'not either pole' holds on the *reservoir* axis (gain↔loss, a dipole — both reservoirs needed) but **not** on the *sector* axis (metric↔topological), which is a **monopole** — the topological charge is net and **owned** by the geometry while the metric carrier is fungible; co-equal as reservoirs, not as character.) This re-weights the core, which narrates the sink as the *invoice* ("structure climbs, cost paid downward"): read ground-first, $L$ is the **Carnot partner** — at $a{=}0$ ($L\\to G\_0$, i.e. $T\_{\\text{cold}}\\to T\_{\\text{hot}}$) no work is convertible and character dissolves **no matter how large $G\_0$**. *Candidate instances:* **(i)** the **Brownian gyrator** — gyrating current $\\propto\\Delta T$, vanishing at equal temperatures: vary the **cold bath alone** (source pinned) and $\\mathcal{A}$ moves, so the sink is **generative, not bookkeeping**; **(ii)** a **trained network** — the weights are the upfront-paid topology (great training cost, then inert storage), and **generation is the kinetic release the sink extracts**, the irreversible decode/commit (the entropy-exporting selection) being where work is actually done, the forward pass the conduit. · **↑** the **sink-sweep** on a real substrate: hold the source fixed, vary the sink, show $\\mathcal{A}$ moves (the gyrator promotes this if run as a one-sided cold-bath sweep) → `battery`. · **✗** holding the sink fixed and varying only the source reproduces the same $\\mathcal{A}(a)$ (the sink is passive bookkeeping; the core's cost-reading is complete and this entry drops); **or** protected content is exhibited *in the conduit* not reducible to {upfront topology} ∘ {sink-extracted work} (the puppet claim fails). · *caution — the **dual loaded-endpoint**:* "**only** the sink is real" is the mirror over-read; collapse either pole and you hit a degenerate face where character does not exist (logged under `shape:apparent-structure-not-observable`). · **→** `character.md` §The conjugate cascade (the sink co-equal, not the invoice), §Isolation (isolation failing \= the sink reaching in); **sibling of** `cascade-ledger-split` (that is the *two-level* coarse-graining split; this is the *single-level* source/sink asymmetry — the gyrator instances this one but not the two-level gate). *Raised 2026-06-13 (the session's conjugate-sink thread).*
* `drive-not-primitive` **[sharpening]** — *verdict:* the foundational word "drive" names no agent — it is the **geometry of the reservoir coupling** the system is placed on (a held difference / slope / placement), supplied externally and **indispensable** (cut it and the geometry is a *fossil* — a shell: morphology and handedness frozen, no σ/𝒜/perturbable response — not purer character). What the system **owns** is a net, one-signed topological charge — the **character-monopole**: 𝒜 = ∮v/D read as the charge of a *circulation* (a curl-source / vortex core; gauge-irremovable Harary-unbalance), around which the current winds; the drive sets only the **spin rate** (J_ss flows with a, the sign/structure drive-independent — the §Amplitude-autonomy result "supplied, not minted" read from the geometry side: one claim in two registers, a **collapse**). **Corrects the over-symmetric gloss** (the sink-draft's "neither pole is primary"): the source↔sink (G₀↔L) axis is a *dipole* — co-equal reservoirs, both needed (`sink-extracts-the-work`); the metric↔topological axis is a *monopole* — net **owned** charge vs fungible balanced carrier, **not** co-equal. **Physics guard (polarity):** the literal ∇V gradient is a *divergence*-monopole = the soft/metric sector (carries no circulation); the object meant is the *curl*-monopole 𝒜 (the vortex charge) — the right intuition is the charge, not the potential field. **Sharper edge — scale-bound character:** the RG climb *up* the cascade discards geometry and keeps the invariant, so the bit's **scale-invariance** (core §Two degrees of freedom — "the circulation bit is the source of the current's scale-invariance") is the *symptom of having projected the geometry away*; character is **scale-bound** (geometric, at a scale), only its protected reading scale-free — the cascade transduces the **reading, not the character**. The governing discipline: geometry is load-bearing, the realization is the payload, the topological bit only its protected projection. · **↑** a runnable discriminator — a substrate where the coarse (topology-only) and fine (geometric) descriptions are **operationally separated by a perturbable response the invariant cannot predict** (geometry carries character the bit doesn't) → `battery`; or 𝒜 = ∮v/D made a forced quantitative statement on a real vortex-bearing substrate. · **✗** the topological invariant **suffices** — every σ/perturbable/response observable recoverable from the coarse bit with the geometry genuinely irrelevant (then character *is* its projection, the geometry-is-load-bearing discipline falls, and "drive" can be a primitive after all); **or** a sustained character on a *single* reservoir (a thermodynamic monopole — refutes the dipole-still-required guard). · *caution:* "there is no drive" overshoots into the frozen-geometry trap; the disciplined form is **owned charge, borrowed spin — and the spin is not optional**. · **→** `character.md` §The conjugate cascade (the monopole owned, the drive borrowed), §Two degrees of freedom (scale-invariance as a projection-symptom), §Branch membership ("the protected observable is the branch" = the protected *reading*; the geometric realization is the character); **sharpens** `sink-extracts-the-work` (dipole on the reservoir axis, monopole on the sector axis) and `selective-coupling-class` (it transduces the reading, not the character). **Core-pass note — landed 2026-06-18:** the difference-of-logs rendering `a = ln G₀ − ln L` is now in core §Control parameter (pure algebra, shown alongside the ratio form); the source-primacy phrasing "gain over loss" stays flagged here pending the monopole reading. **Sibling:** `selective-coupling-class` (the cross-stratum coupling — the cascade transduces the *reading*, the geometry staying scale-bound). *Raised 2026-06-13 (the "there is no drive / the gradient is a monopole / coarse-grain away geometry and it isn't character" thread).*

### Battery — executable falsifier specs

* `battery:seam-collapse` **\[battery\]** — *verdict:* the amplitude/drive-seam scaling-collapse battery; runnable on synthetic now, the forcing needs a real substrate with a measurable FDR. Target invariant $(\\alpha, F)$, not a coefficient series. · **↑** stable $\\alpha$ \+ universal collapse \+ substrate-family profile classes on a real substrate → promotes `seam-classification` (the thm9 crossover-width instance). · **✗** width saturates as $D\\to\\infty$; or no single $\\alpha$; or equal $(\\alpha,w)$ with incompatible profiles. · **→** receipts §Deformation calculus.  
* `battery:sign-interior` **\[battery\]** — *verdict:* the protected-sign interior-circulation kill — interior measurement only (never the never-attained boundary), reading the binary sign of the affinity, not a graded triality. Vindicated on rock-paper-scissors (a real substrate carries a protected sign on a directed 3-cycle, drive-independent, flipping only by rewiring; the symmetric null carries none). · **↑** a protected sign on a real substrate always sits on a frustrated triad → promotes the central commitment *(fired — crossed)*. · **✗** a real substrate with a protected sign and no triad; or the synthetic sign flips under finite drive variation without rewiring. · **→** the protected current; receipts §Central commitment, §Two bits.  
* `battery:wall-ladder` **\[battery\]** — *verdict:* the plateau-ladder falsifier — a cascade's levels are normally hyperbolic plateaus separated by the three boundary kinds; the discriminator is the CLV minimum angle $\\theta\_{\\min}$ \+ the smallest transverse exponent. Both faces demonstrated: the plateau side on the coupled Stuart–Landau cascade ($\\theta\_{\\min}$ bounded, false-positive controlled), the closure-loss side via a Benjamin–Feir route ($\\theta\_{\\min}\\to0$, $\\lambda\_{\\max}\>0$). · **↑** on a real cascade: levels read as plateaus, a level at $\\varepsilon\\to1$ shows $\\theta\_{\\min}\\to0$ → promotes `wall-as-type-boundary`. · **✗** the discriminator fails, or a real cascade level is not such a plateau. · **→** `wall-as-type-boundary`; coarse-graining. The canonical delay-driven mechanism stays the deferred test.  
* `battery:dimensionless-self-probe` **\[battery\]** — *verdict:* the dimensionless self-probe ($\\mathcal{T}\\ge1$)  
  + the canonical affinity, instanced on rock-paper-scissors (the empirical winding affinity closes onto the forced form to 1.7%, and the affinity and $\\omega/\\gamma$ are noise-independent). Scope: the noise-independence is the symmetric-multiplicative-drive case (a thermodynamic-force drive would make the affinity drive-dependent). · **↑** a second real substrate with the same noise-independent affinity → promotes the self-probe ruler. · **✗** the affinity or $\\omega/\\gamma$ noise-dependent on a real substrate, or the self-probe fails to close. · **→** the self-probe ruler; promotes `dimensionless-substrate`.  
* `battery:scale-covariant-circulation` **\[battery\]** — *verdict:* the topological bit on the scale axis — the same circulation read at two scales splits into a scale-**invariant** affinity (the bit) and a scale-**covariant** magnitude. Instanced: a legitimate adiabatic projection leaves the affinity invariant in sign and value while the current renormalizes exactly by the eliminated-mode timescale; the illegitimate maps (driven fast subspace; loop contraction) are the pre-excluded failures. · **↑** the split shown on a continuous affinity-bearing substrate → promotes the scale-relativity claim. · **✗** under a legitimate map the affinity flips or erases. · **→** scale-relativity; coarse-graining.  
* `battery:two-survivals-plane` **\[battery\]** — *verdict:* branch survival ($\\Delta V$, the basin-escape quasipotential) and current survival ($I(0)$, set by the cycle affinity $\\mathcal{A}$) are **independent axes**; substrates fill all four cells, so "no protected current" is not "no dynamical landscape." Already core: **current-only** (structurally stored — bare RPS, fuel-driven DNA: $\\mathcal{A}\\ne0$, mirror reachable only by rewiring) and **both** (spontaneously selected — homochiral: $\\mathcal{A}\\approx21.8$, $\\Delta V\\approx0.018$; **second instance** the co-handed twin-cycle via exchange/$S\_2$ SSB, not parity). New corners: **branch-only** (metastable — a symmetric Hopfield attractor net gives $\\Delta V\\approx0.97$ with $\\langle\\sigma\\rangle=\\mathcal{A}=0$ to machine precision, real Jacobian; `experiments/hopfield_corner.py`), the mirror of bare RPS; **neither** (soft-metric — a 2-layer feedforward net on a Gaussian mixture: measured capability $I(\\hat Y;Y)\\approx2.9$ bits → the Bayes ceiling, the hard sector **structurally** absent — node-adjacency nilpotent ($\\mathcal{A}\\equiv0$ by acyclicity), $\\Delta V$ undefined for want of recurrence; `experiments/neither_corner.py`), the first *non-trivial* substrate with no hard bit. Column boundary \= **frustration** ($\\mathcal{A}:0\\to\\ne0$); row boundary \= a **coexisting mirror** (rewiring → thermal crossing). Corollary: soft-sector capability is silent on the hard sector — the most *informative* corner carries no protected current at all. · **↑** *(crossed)* — all four corners instanced → core §The two-survivals plane \+ receipts §Two-survivals plane. · **✗** a symmetric/gradient attractor net with $\\mathcal{A}\\ne0$; or a feedforward/acyclic substrate carrying a sustained protected current; or branch survival and current survival shown *not* independent on a real substrate. · **→** core §Branch membership \+ the fidelity–protection split ("three layers, not one"). `pa:cycle-affinity`.

### Staked — planted in the core, un-instanced (mirrors the receipts `staked` tag — I1)

* `staked:gfdr-two-step` **\[staked\]** — *verdict:* near-threshold FDR is two-step; a short-lag $X{=}1$ is not below-threshold. · **↑** a second instance — recurrence of the confusion, or a real glass/quantum/brain substrate exercising the two-step. · **✗** a substrate where short-lag $X{=}1$ genuinely *is* below threshold. · **→** receipts §Two FD frames — two-step.  
* `staked:auto-tuning` **\[staked\]** — *verdict:* a substrate's slow rates auto-tune to the diagonally-stabilising weight $W=\\mathrm{diag}(\\gamma\_{\\text{ref}}/\\gamma\_{s,i})$. · **↑** a substrate requiring diagonal stability shown to auto-tune to the inverse form. · **✗** a stable maintained state on a non-PSD frustration-free topology whose measured rates give an indefinite $W\\gamma+\\gamma^TW$. · **→** receipts §Auto-tuning.  
* `staked:memory-collapse` **\[staked\]** — *verdict:* $\\beta\\approx1-\\varepsilon$ near the marginal point. · **↑** $\\beta(\\varepsilon)$ measured near the marginal point on a real hierarchical substrate, linear with both endpoints respected. · **✗** $\\beta$ departs from $1-\\varepsilon$. · **→** receipts §Memory collapse; `character.md` Coarse-graining. · *shared substrate (2026-06-13):* a **sleeping nervous system** is a candidate for both this and `consolidation-ascent`; the duty cycle's **gap-burstiness** is a third, independent register on the same $\\beta$ (alongside the aging exponent and the kernel), tightening the **$\\beta$-collapse** — the sharpest falsifier — when measured there.

### Recently crossed the line

Promoted this campaign, now in the core (recorded in receipts §Corrections and promoted refinements): the **central commitment** and **two-frame construction** (on rock-paper-scissors); the **deformation chart** and **homochirality** (on rock-paper-scissors and the homochiral triad); the **exact two-frame magnitude identity** (a forced-not-fitted derivation on the rotational-OU testbed); **composite branch** (on the fuel-driven DNA reaction network — the manifold and closure lifts both crossed, now `character.md` §The space of characters and §Composition under coupling); and **branch survival** (the racemic-saddle quasipotential $\\Delta V\\approx0.018$ on the homochiral triad, separated from $I(0)$ by noise-scaling and by controlled embedding — receipts §Branch-survival barrier). The $\\mu$-sweep is run: branch survival is born at $\\mu\_c=(1+a+b)/3=0.833$ by **competitive exclusion**, with $\\Delta V\\propto(\\mu-\\mu\_c)$ linear (the pitchfork $(\\mu-\\mu\_c)^2$ superseded) and zero below threshold. **Both owed items now paid (receipts §Branch-survival barrier):** the multiplicative-noise robustness (demographic $\\sqrt{x}$ noise — $\\Delta V$ rescales to $\\approx0.273$ but existence and protection survive, so not an additive artifact), and a second independent thermalized-crossing substrate — the **co-handed twin-cycle** ($\\mathcal{A}\\approx21.8$, $\\Delta V\\approx0.018$), which instances `both` through a spontaneously broken **exchange** ($S\_2$) symmetry, *not* parity. Decisive independence: sign($\\mathcal{A}$) is preserved across the branch flip (both clusters co-handed) where the homochiral parity flip reverses it — the two survivals shown orthogonal a way the parity instance structurally cannot. The competitive-exclusion mechanism is **handedness-blind** (μ-sweep): $\\mu\_c$ and the linear $\\Delta V(\\mu)$ are machine-precision identical across exchange and parity, since the breaking mode is uniform-within-cluster — so the open pitchfork-vs-competitive-exclusion question resolves the same under both SSB types. `experiments/twin_cycle_corner.py`, `twin_mu_sweep.py`. And **`both` across a second bifurcation mechanism**: an autocatalytic (Kondepudi–Nelson) substrate gives a genuine **soft pitchfork** — $ee\_*^2\\propto(k\_{1c}-k\_1)$ linear ($R^2{=}1$), barrier $\\Delta U\\propto ee\_*^4\\propto(k\_{1c}-k\_1)^2$ **quadratic** (vs the twin's hard/linear); adding an $a\\neq b$ 3-cycle opens a **coexistence window** $ec\\in\[0.05,0.20\]$ that mints $\\mathcal{A}\\approx0.6$–$3.5$ nats while the soft pitchfork holds ($ee\_*^2$-$R^2{=}1$, $\\Delta U\\propto ee\_*^4$ $R^2{=}0.997$, reset \~50/50); past $ec\\gtrsim0.25$ the cycle destabilises the branch (it *participates* in the bifurcation, not merely decorates it). Supported claim: **symmetry breaking does not fix the barrier scaling — the saturation mechanism does** (two distinct constructions, not yet "mechanism-independent"; receipts §Branch-survival barrier, §Two-survivals plane). `experiments/autocat_pitchfork.py`, `autocat_both.py`. **Cross-stratum minting (2026-06-18):** the union-cycle holonomy form (`𝒜_union = ∮Γ_AB·dx`, exactly the cross term when the parts are non-circulating), the skew-coupling fact (no metric work, but necessary-not-sufficient — a Hamiltonian rotation is skew yet `𝒜 = 0`), and stratum-agnostic minting promoted to core as forced derivations (§The minting claim, §The space of characters, §Motion and proximity, §Control parameter; receipts §Composition). The smallness regime, the depth law, and the substrate stay below the line in `selective-coupling-class`. Also landed (clarity / forced consequence): the **two irreducible sectors** compression (abstract, §Object, falsifier added — a character is two non-interconvertible kinds of information, all other observables their readings) and the **cascade closure consequence** (no new *kind* of information up the tower; §Cascade; receipts §Two irreducible sectors).

---

## Tombstones — discarded false starts (kept so they are not re-explored)

* `flip-cost-across-zero` — protection-as-a-flip-cost across the sign-erased zero. **Died:** formulating protection as a measurement that visits the affinity-zero is malformed by the open-interval discipline (the zero is never attained). **Replaced by:** the interior sign-stability test (`battery:sign-interior`). The $\\ge1$-bit flip floor fired entirely — a sign flip is a reversible bijection (no erasure, no Landauer floor).  
* `sign-kill-bisection` — the can-kill-can't-vindicate standdown that split the test into kill-only. **Died:** a boundary-gated formulation; the framework is degenerate at the boundary, rich in the interior. **Replaced by:** the interior, can-kill-and-vindicate `battery:sign-interior`.  
* `thm9-coefficient-series` — the Boolean-deviation bound→equality as an analytic $1/D$ coefficient series. **Died:** two passes converged that the seam is a crossover scaling function, not a series; the series stayed fitted-not-forced. **Replaced by:** the scaling-collapse target $(\\alpha, F)$.  
* `alignment-dependent-active-stress` — the alignment-dependent form for the active-stress fingerprint. **Died:** it captures only the flocked-state correction, not the leading scaling. **Replaced by:** the alignment-independent fingerprint $\\beta/\\alpha\\sim v\_0^2\\tau\_R/D\_{\\text{trans}}$.  
* `quadratic-lyapunov-candidate` — a quadratic Lyapunov form. **Died:** fails the critical-point test ($\\partial V/\\partial x\_i|\_{x^\*}\\ne0$). **Replaced by:** the relative-entropy form (receipts §Frustration-free Lyapunov).  
* `current-aids-escape` — the protected current read as a *resource that lowers the branch-escape barrier* ($\\Delta V$ falling with the affinity $\\mathcal{A}$). **Died:** the exact instanton (gMAM, validated on Maier–Stein to $0.01%$) gives a $\\sigma\\to0$ quasipotential **flat** in $\\mathcal{A}$ ($\\Delta\\hat S\_H \=-0.4%$ over $\\mathcal{A}:0\\to21.8$ vs the finite-$\\sigma$ slope's $-17%$), the instanton never bending off the symmetric subspace — because the current is **exactly transverse** to the escape mode at the saddle ($|\\cos(J,e\_u)|\\sim10^{-15}$, symmetry-protected: escape and current in different irreps, broken $\\propto\\delta$ by a $Z\_3$ break). The current's own ✗ ("gMAM shows the finite-window slope mis-ordered the true actions") fired. **Replaced by:** an imported reading — the **transverse-decomposition theorem** leaves $\\Delta V$ invariant; the measured drop is the **irreversible Eyring–Kramers prefactor** (receipts §Branch-survival barrier; `pa:transverse-decomposition`). The two-survivals separation is *reinforced* ($\\Delta V\\perp\\mathcal{A}$ by a symmetry theorem); a current reaches a barrier only where coupling mixes the irreps (the in-substrate Maier–Stein turn-on, `gmam_mixing_test.py`).

