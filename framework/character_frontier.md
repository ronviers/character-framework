# Character — frontier (the maturity ledger)

The fourth axis. The other three say *what is claimed* ([`character.md`](http://character.md)), *how it is derived* ([`character_receipts.md`](http://character_receipts.md)), and *what is borrowed* ([`character_prior_art.md`](http://character_prior_art.md)). This one says **how settled each thing is, and exactly what would move it** — the single source of truth for everything not yet load-bearing. Its job is not to feed the core but to keep provisional work *out* of it until it earns in: the structure here is quarantine, not on-ramp. The value of `character.md` is that everything in it is committed; that guarantee survives only if the line below is strict.

---

## Dashboard — scannable index *(get caught up here; the full entries, with ↑ promote / ✗ kill / → target, are below)*

*The full index — one row per entry, grouped by rung. Scan for your substrate or question, then drop to the dense entry below for the verdict, the promote-gate (↑), the kill-condition (✗), and the core target (→). This is a **navigation layer on top**: nothing below is moved or deleted; every entry is preserved verbatim. The table is lossy by design (one-line verdicts), the doc lossless. Rungs:* `steeping` *(speculation) →* `sharpening` *(a mechanism or test exists) →* `battery` *(a runnable falsifier) →* `staked` *(planted in the core, un-instanced) →* crossed *(now in* `character.md`*); plus* `nudges` *(reader-steering) and* `tombstones` *(killed, kept so they are not re-tried).*

### Steeping

| entry | one-line verdict |
| :---- | :---- |
| `flow-resident-number` | a number-type whose identity is a non-terminating flow-trajectory — **static-value/centroid reading resolved 2026-06-28 (a derived statistic → tombstoned; real object = the stable foliation frame $E^s$ = existing transverse/marginal machinery); residue = the cyclic character (`consolidation-ascent`)** |
| `fractional-operator-algebra` | an operator algebra on memory regimes β∈(0,1\], recovering the integer-β operators at β=1 |
| `two-reframe-parallel` | the Boolean limit (D→∞) and the marginal point (ε→1) share architecture — is there a third axis? |
| `secondary-regime-operators` | damping / phase-lock / coarse-graining trichotomies as further operator-algebra candidates |
| `post-threshold-universality` | a candidate mechanism for the seam-width law — none found yet |
| `palm-self-probe` | the two-frame iff-chain re-projected into queueing (time-stationary vs Palm / event-conditioned average) |
| `epsilon-hub-transport` | one transport law on the distance-to-marginal ε, read across four registers |
| `affinity-hub-transport` | one transport law on the affinity a, read across three registers |

### Nudges *(reader-steering; HELD)*

| entry | one-line verdict |
| :---- | :---- |
| `nudge:loaded-endpoint-descent` | read a loaded phenomenon (sleep, a market, a mood) by descent to its substrate skeleton — periodicity ≠ the protected bit; only the protection test certifies it |

### Shapes *(cross-track recurrence classes; navigation)*

| shape | one-line |
| :---- | :---- |
| `shape:apparent-structure-not-observable` | apparent structure (periodicity/capability/figure/endpoint) ≠ the protected bit; the dual: don't over-credit *either* pole — the gradient carries the bit. Recurrence fired (2026-06-13) → `nudge:loaded-endpoint-descent` HELD review due |

### Sharpening

| entry | one-line verdict |
| :---- | :---- |
| `predictor-soft-sector-blindness` | **[steeping→sharpening 2026-06-28]** confident-and-false on hard-sector questions = the absent rejection operator (I−P); learns the interior, not the codim-1 boundary where the branch flips |
| `seam-classification` | every deformation-calculus threshold is one seam via singularity theory (singular set + codimension + universal unfolding); the rounding exponent & hard-vs-soft are the normal form; metric→singularity→topology — the protected sign = the non-unfoldable stratum (**crossed**) |
| `dimensionless-substrate` | the dimensionless self-probe ruler, instanced on rock-paper-scissors; needs a 2nd real substrate |
| `consolidation-ascent` | one heat-tax mechanism, three faces (offline duty-cycle / compression-vs-maintenance / tower-ascent = consolidation); survivable only at β<1, locked to the β-collapse; consolidation = thermalized→external-rewiring conversion; candidate substrate = a sleeping nervous system; limit object = a cyclic character |
| `frustration-ascent` | the generative-recursion bet — generative of chirality/topology (mintable), parasitic on the drive (amplitude autonomy supplied, not minted); evaded by a two-level cascade |
| `self-referential-closure` | the alive-loop operation stack: minting (HAVE) → templating (MEASURED degenerate — faithful 1-bit kernel, von Neumann channel I saturates at C≈1 bit ≪ k) → self-wiring (the recursion writes its own coupling graph; minting goes endogenous — owed, bigger); heredity = templating over self-wiring |
| `wall-as-type-boundary` | the marginal point is a ladder of normally-hyperbolic plateaus joined by three boundary kinds (ignition / topological flip / closure-loss) |
| `chirality-protection` | the protected object is the gauge-irremovable affinity / graph-flux sign — not the exceptional pair, not an integer charge |
| `cascade-ledger-split` | the conjugate cascade's two ledgers are one coarse-graining entropy-production split (σ\_tot \= σ\_res \+ σ\_hid) |
| `qec-transverse-decomposition` | **the TDT** — metric⊥topological computed in the surface code \+ glass \+ triad: the protected current is barred from *all* metric observables; symmetry-protected, not generic |
| `selective-coupling-class` | a 3rd coupling class — hard-sector-rich / soft-sector-poor (passes circulation, suppresses population mixing); two tiers — symmetry-exact (\= core) vs smallness 𝒮\=\|Γ\|/\|V\| (new); payload \= a cascade-depth law N(𝒮); absorbs the retired `cross-stratum-transduction` |
| `cascade-climbing-universality` | cascade-climbing has a universality class (candidate: KPZ); KPZ-class ⇔ driven (𝒜≠0), EW-class ⇔ detailed-balanced — the growth exponent as a soft-sector readout of the hard-sector affinity |
| `sink-extracts-the-work` | the sink is first-class, co-equal with topology: topology paid upfront and inert, conduit is a puppet, work extracted at the cold sink; falsifier \= the sink-sweep (vary sink, hold source, 𝒜 moves) |
| `drive-not-primitive` | 'drive' is reservoir-geometry the system sits on, not an agent (owned charge, borrowed spin); what's owned is a net topological charge — the character-monopole 𝒜 (a curl-source) — and coarse-graining climbs out of the geometry, so the cascade transduces the *reading*, not the character |
| `universal-invariant-classification` | is sign(𝒜) a universal invariant or per-substrate? clean negative on the *import* (no model-independent classifying structure — Polettini gauge-cohomology + Harary balance are intra-substrate); residue = a composition law — **built** (`composition_law.py`): the cycle-space homology import, existence-bit composes (algebra) but value-bit is rate-dependent ⇒ **C-middle** (metaphor averted, category not won) |
| `escape-degenerate-replication` | the pre-life threshold reframed: replication is cheap (degenerate, ~1 bit), so the question is how to **escape** it — object → dissipative medium → ecology → a genuine **environment** ("a place to be fit in") the instant something **mints a bit that reads it**; environment = info × reader (relational, not anthropic); + a falsifiable scale-of-character prediction (gravity-cast strata window) and gravity-contingent carbon; value = problem-formulation, not a result |
| `reading-transition` | the abiogenesis arc's novel object — a **reader** = a protected loop steered by ecological distinctions. Reading splits like reproduction: cheap (metric — the cross-rule, pre-heredity) vs hard (bit-level — superselection-barred, only across replacement = proto-heredity); order parameter R\_hard over a **lineage**; selection = the unique metric→topological channel. Measured by `battery:readability`; first real-data landing on Syn3A — the reader is the dominant archive cross-term (≈41% of bits, the seam where the cut blurs) |
| `circulation-held-capacity` | Organization = circulation-held K(C) + archive-held; K(C)=K\_topo(b1)+K\_metric. Anchors forced-not-fitted: KaiABC K=2, PO K=4, + QEC (keep=b1=#logical-qubits literal, wall=channel capacity). **Archive-term MEASURED on Syn3A**: the cut BLURS at the reader (≈41% of bits) — the two-term *sum* disfavored at high archive (overlap at the self-referential seam). **PROMOTED TO CORE 2026-06-25** (§The keep and the archive; archive-term credited to KW/Rosen/Pattee); frontier holds the residue (K\_metric always 1; one biochemical class; K\_topo not uniformly defined) |
| `reader-by-overreach` | dynamical path keep-ceiling→reader (overreach→failure→trace→archive→reader); reader = a **metric→topological back-channel** selected when consulting a trace beats re-minting (incl. reader upkeep) at the maintenance wall. **Deferral audit → defer the concept** (owned in pieces). Residue = two integrations (crossover ↔ the `N log N` maintenance wall; sector reading predicts Syn3A's reader-dominated archive) + the **coordination⊥reading** anti-correlation. **Disconfirmer #3 MEASURED** (`syn3a_reader_upkeep.py`): reader-upkeep ≈46%, co-equal with circulation, NOT triggered. Accuracy-is-fitness sharpening (steeping): accuracy = the keep's fitness (Eigen sans replication); archive minted at topological-active correction; upkeep = the fidelity price. Gate: don't build synthetic |
| `engine-character-onset` | the engine→character crossing is a **topological onset, not a bifurcation** — 𝒜 on the cycle space H₁ (Schnakenberg/Polettini), minted discontinuously when a driven frustrated cycle closes (not ramped on by harder driving). **Settled by import** (already core §Minting); transition-kind **crossed to core 2026-06-25** (§Motion and proximity: continuous N=2 bifurcation vs discontinuous N≥3 rewiring). Only the *occurrence* on a real engine (= the abiogenesis bootstrap) is open |

### Battery — executable falsifier specs

| entry | one-line verdict |
| :---- | :---- |
| `battery:seam-collapse` | the amplitude/drive-seam scaling-collapse falsifier; target invariant (α, F); needs a real substrate with a measurable FDR |
| `battery:sign-interior` | the protected-sign interior-circulation kill; vindicated on rock-paper-scissors (drive-independent sign, flips only by rewiring) |
| `battery:wall-ladder` | the plateau-ladder falsifier; discriminator \= CLV minimum angle; both faces shown on the Stuart–Landau cascade |
| `battery:dimensionless-self-probe` | the dimensionless self-probe \+ canonical affinity on rock-paper-scissors (closes to 1.7%, noise-independent) |
| `battery:scale-covariant-circulation` | the same circulation at two scales splits into a scale-invariant affinity (the bit) \+ a scale-covariant magnitude |
| `battery:two-survivals-plane` | branch survival (ΔV, basin escape) ⊥ current survival (𝒜) — independent axes, all four corners filled *(crossed)* |
| `battery:chiral-rotor-triad` | the minimal mechanical instance — 3 chiral rotors in a driven ring (Δφ=2π/3), circulation sign locked to shape-chirality χ; run synthetically (calibration), all 3 kill-switches pass (C≈3.4χ, SNR≈450; sign parity-locked not drive-locked; chiral 0.75 vs 8e-16); deterministic triad locks → current is noise-activated hopping; physical build still owed |
| `battery:strata-current` | sort-vs-mint on a driven stratified medium (sweep stratification s vs cyclic wiring Ω, `strata_sort.py`): sorting rises but 𝒜≡0 — no separation mints; a protected current needs frustrated wiring and is sort-independent (flips only on rewiring); strong sorting quenches \|J\|, not sign. Engineering separation corpus = salience, all on the sorting axis ⇒ ✗-harvest a whole class as *stage*; ↑-scan = a sort-independent inter-strata circulation at the Batchelor–Ozmidov scale |
| `battery:readability` | **built + run** (`experiments/readability.py`): metric readability ∂(occ/\|J\|)/∂d turns on generically (cheap reading); bit readability ∂(sign𝒜)/∂d \= 0 to machine precision within a lifetime (superselection null — gradient is curl-free); R\_hard \= ∂⟨sign𝒜⟩/∂d over a lineage \= 0 at symmetry-break δ\=0, ≠0 for δ>0 (∼linear onset, TDT move 3); co-emergence — R\_hard≠0 only with **both** replacement + symmetry-break. All kill-switches pass synthetically; real-substrate reader owed |
| `battery:circulation-capacity` | **built + run** (`experiments/kaiabc_capacity.py`) on KaiABC: both make-or-breaks cleared -- forward limit-cycle current J\_cyc=+0.025 (collapses at drive-off, structure-locked, Kirchhoff-equal), b1=1, Floquet `[1, 0.046, 7e-5]` -> one marginal phase => K=b1+phase=2>1; feedback-generated, not a frozen-rate bias. **2nd substrate landed**: PO reaction (`bfso_capacity.py`, BFSO model) K=b1(3)+1=4, distinct via two coupled enzyme loops |

### Staked — planted in the core, un-instanced

| entry | one-line verdict |
| :---- | :---- |
| `staked:gfdr-two-step` | near-threshold FDR is two-step — a short-lag X=1 is not below-threshold |
| `staked:auto-tuning` | a substrate's slow rates auto-tune to the diagonally-stabilising weight W \= diag(γ\_ref/γ\_s,i) |
| `staked:memory-collapse` | the memory exponent β ≈ 1−ε near the marginal point (tracks the contraction modulus) |

### Recently crossed — now in `character.md`

| claim | substrate it crossed on |
| :---- | :---- |
| central commitment · two-frame construction | rock-paper-scissors |
| deformation chart · homochirality | rock-paper-scissors \+ homochiral triad |
| exact two-frame magnitude identity | rotational-OU (forced-not-fitted) |
| composite branch (minting \+ sustained circulation) | fuel-driven DNA reaction network |
| branch survival (ΔV; competitive-exclusion, linear in μ−μ\_c) | homochiral triad (+ twin-cycle 2nd instance via S₂; autocat soft pitchfork) |
| protection re-derived (the protected sign = the non-unfoldable stratum; metric→singularity→topology) | forced composition (Harary graph-flux + `singularity-stratification`); §The two tangent sectors |

### Tombstones — killed, kept so they are not re-tried

| entry | died / replaced by |
| :---- | :---- |
| `flip-cost-across-zero` | protection as a flip-cost across the affinity-zero — boundary-malformed → interior sign-stability test |
| `sign-kill-bisection` | the kill-only standdown — boundary-gated → interior can-kill-and-vindicate battery |
| `thm9-coefficient-series` | Boolean-deviation as a 1/D series — it is a crossover scaling function → (α, F) collapse |
| `alignment-dependent-active-stress` | alignment-dependent active-stress fingerprint — only the flocked correction → alignment-independent fingerprint |
| `quadratic-lyapunov-candidate` | a quadratic Lyapunov form — fails the critical-point test → relative-entropy form |
| `current-aids-escape` | protected current as a barrier-*lowering* resource — gMAM shows ΔV flat in 𝒜 (transverse) → the transverse-decomposition prefactor reading |
| `rejection-anchor-as-new-invariant` | the rejection field's barycenter / ∫n dσ / a "flow-resident number" as a *new* invariant — ∫n dσ≡0 (fake-NaN), the centroid is a derived statistic, no closed current → the stable foliation $E^s$ = existing transverse/marginal machinery |

---

## Gates — the state machine

Five rungs and a hard line. An item occupies exactly one rung at a time; a rung change is an explicit event with a recorded trigger (no lore promotions).

```
steeping ──▶ sharpening ──▶ battery ──▶ staked ──╮
   │             │             │           │      │  COMMIT LINE
   ▼             ▼             ▼           ▼      ▼  ───────────────
 discarded (tombstone) at any rung      promoted → character.md / receipts / prior-art
```

**Promotion gates** (the condition to cross each boundary):

- `steeping → sharpening` — the speculation acquires *either* a concrete mechanism *or* a way to test it. Pure intuition with neither stays steeping.  
- `sharpening → battery` — a runnable falsifier spec exists: finite operating point, a forced-not-fitted target, explicit kill **and** vindicate conditions.  
- `battery → staked` — the spec is planted in the receipts as a falsifier of record (the `staked` tag), un-instanced by design.  
- `staked → promoted` *(crosses the line)* — requires all of: (a) a **real cross-substrate instance** (a synthetic pass is calibration, never vindication), or for a non-instance claim a **forced-not-fitted derivation**; (b) the move **shrinks** the falsifiable surface, never enlarges it; (c) for a refinement that *defends* an existing claim, a **second independent instance**.

**Demotion / kill gates:** an item whose promotion gate is disproven goes to `discarded` with a one-line tombstone (so it is never silently re-explored). A `staked` falsifier that **fires** does not merely demote — it kills the core claim it defended, and the kill propagates into the core.

**Schema (mandatory).** Every entry carries: `key` **\[rung\]** — *verdict* (current belief, one line) · **↑** promote-gate · **✗** kill-condition · **→** core target.

---

## Invariants — auditable

- **I1 — bridge integrity.** Every `staked` receipt ↔ exactly one frontier entry at `[staked]`. The staked rung is the only place an item is simultaneously in the core and on the frontier.  
- **I2 — no orphans.** Every entry has a `→` target, or is `pure-exploration` (steeping only).  
- **I3 — purity of the core.** Nothing above the line lives in a working file; nothing below it lives in `character.md`/receipts/prior-art. The core contains only what crossed.  
- **I4 — recorded triggers.** No rung change without a logged trigger meeting the named gate.  
- **I5 — self-application.** The framework is itself a dissipative register-system, so its own claims face the filters it applies to substrates. Every `staked`/`promoted` claim must pass three: **collapse** (over-determined — a binding or falsifier, never a single-register identity); **iff-chain** (its architecture is a stated entailment, not an asserted axiom); **flow** (every dynamical quantity moves with the operating point — no inert constant). A claim failing any sits in the framework's own metaphor regime and cannot stake.  
- **I6 — no silently-committed conjectures.** A core claim carrying a falsifier must be in one of three states: its falsifier has a recorded run, it is a forced-not-fitted derivation, or it is explicitly `staked`. A claim with an un-run falsifier and no derivation that is neither `staked` nor on the frontier is malformed and demotes. (This is the gate a boundary-import claim slips when no interior apparatus exists to run it: the antidote is that boundary behaviour is an entailed theorem of the interior, never an un-run import asserted at the limit.)

---

## Register

### Steeping — speculation (no commitment)

* `flow-resident-number` **\[steeping\]** — *verdict:* a candidate number-type whose identity is a non-terminating trajectory under the coarse-graining flow (the affinity-as-orbit, the slow-manifold trace, the running $\\beta$-functions); value-readings are static projections. · **↑** a closed composition algebra under the flow \+ a natural metric. · **✗** no closed algebra. · **→** `pure-exploration`. The algebraic side of the generative-recursion question (paired with `frustration-ascent`). **Resolved in part (2026-06-28, two outbound rounds):** the "value-readings are static projections" reading was right — the static value (barycenter/centroid) is a *derived coordinate statistic*, tombstoned (`rejection-anchor-as-new-invariant`); the real flow-resident object is the **stable foliation / Oseledets frame $E^s$** = the existing transverse/marginal machinery (`pa:oseledets-pesin`, receipts §Rejection-anchor), real only with a spectral gap (dissolves at the marginal point). Live residue = the **cyclic character** (a limit cycle through ℭ, `consolidation-ascent`), not a new number.  
* `fractional-operator-algebra` **\[steeping\]** — *verdict:* an operator set on memory regimes $\\beta\\in(0,1\]$ recovering the integer-$\\beta$ operators at $\\beta=1$; the algebra of *coupled* fractional systems is open. · **↑** a closed algebra at finite $\\beta$ \+ a deformation-calculus analog \+ boundary rules at $\\beta\\to1^-$. · **✗** no closed algebra. · **→** a memory-regime operator layer.  
* `two-reframe-parallel` **\[steeping\]** — *verdict:* the Boolean limit ($D\\to\\infty$, static) and the marginal point ($\\varepsilon\\to1$, dynamical) share architecture; watch for a third axis. · **↑** a third concrete reframe axis, instanced. · **✗** the parallel is cosmetic. · **→** `pure-exploration`.  
* `secondary-regime-operators` **\[steeping\]** — *verdict:* damping, phase-locking, and coarse-graining regime trichotomies as further operator-algebra candidates; memory regimes are the cleanest first. · **↑** a closed algebra on any one trichotomy. · **✗** no closed composition. · **→** `pure-exploration`.  
* `post-threshold-universality` **\[steeping\]** — *verdict:* a candidate seam mechanism, no mechanism yet. · **↑** a concrete link to the seam-width law. · **✗** no link survives. · **→** `pure-exploration` (feeds `seam-classification` · the thm9 crossover-width instance / `battery:seam-collapse`).  
* `palm-self-probe` **\[steeping\]** — *verdict:* the two-frame iff-chain re-projected into queueing — the time-stationary average (external) and the Palm / event-conditioned average (self-probe) are an inequivalent pair; Poisson arrivals are the degenerate $X\\equiv1$ case. On a non-reversible cyclic queueing network it reinstantiates: self-probe defined $\\Leftrightarrow$ net circulation $\\ne0 \\Leftrightarrow$ routing affinity $\\ne0\\Leftrightarrow$ a directed routing cycle. · **↑** resolve the self-probe-current crux (the protected current is the routing-cycle affinity, not the arrival rate) \+ a spec on a 3-station cyclic network. · **✗** a non-reversible cyclic queueing NESS where the two frames agree despite affinity $\\ne0$, or disagree with no routing cycle. · **→** the two-frame construction (new projection) \+ the queueing adoption row.  
* `epsilon-hub-transport` **\[steeping\]** — *verdict:* a transport law on the distance-to-marginal-point $\\varepsilon$ — one $\\varepsilon$ in four registers (aging $\\beta\\approx1-\\varepsilon$, heat-tax $\\alpha(\\varepsilon)$, branching $\\to1$, critical length); degenerate at both endpoints, content in the interior. · **↑** a runnable collapse spec measuring $\\ge2$ registers and inverting to a common $\\varepsilon$. · **✗** the maps are mutually inconsistent, or two registers are tied definitionally. · **→** the memory-exponent and SOC falsifiers; coarse-graining.  
* `affinity-hub-transport` **\[steeping\]** — *verdict:* a transport law on the affinity $a$ — one $a$ in three registers (queue load $-\\ln\\rho$, relaxation-oscillation $Q(a)$, phase-lock suppression); degenerate at both ends, signal in the mid-band. · **↑** a runnable collapse spec inverting $Q$ and phase-lock suppression to a common $a$. · **✗** the two inferred $a$'s cannot agree by construction. · **→** the queueing adoption row \+ the relaxation-oscillation and phase-locking claims.  

### Nudges — steering candidates (a parallel doc-gate track; HELD)

A **nudge** corrects how character *steers a reader* — a sentence the core is missing or mis-places, diagnosed from a misread — not a claim about a substrate. It does **not** climb the world-rungs above (no substrate, no falsifier-instance); its path is two-rung: `candidate → (recurrence: a 2nd independent misread trips the same wire) → core`. The `↑` is recurrence; the `✗` is "the existing core text already steers correctly — the miss was reader sloppiness." Each entry additionally logs its *trigger(s)* (the misreads \= its support) and the *shape* it instances (the steering-failure class).

**Policy — HELD.** No nudge promotes to core yet, even one that already looks recurrence-mature. Accumulate the corpus and read where the shapes cluster *first*; the holding pen exists to see the shape before committing a sentence. Promotion is a deliberate later event, never automatic on the gate.

* `nudge:loaded-endpoint-descent` **\[nudge\]** — *verdict:* a loaded macro-phenomenon (sleep, a market, a mood) is read by **descent** to its substrate-general skeleton, never directly — `ℭ` is degenerate at loaded endpoints exactly as at the boundary faces ${0,1,\\infty}$. Two steering misses. **(A) Periodicity is not the hard bit** — an observable oscillation establishes neither a protected current nor a minted bit; only the *protection test* does (noise-independent $\\mathcal{A}$; a sign that reset-and-re-drive shows is wiring-set or re-rolled). The §Composition gate ("the spectral count is circular; the non-circular observable is the protection test") sits *downstream* of the minting language instead of guarding it, so a reader walks past it. **(B) Decompose before composing** — a switch is **branch-only**, an oscillator at most **current-only**; their co-occurrence is two corners, not a `both`, until the union graph's frustration is exhibited and the composite's current *measured*. · **↑** recurrence — a 2nd loaded-endpoint misread tripping the same wire (then a HELD-policy review, not auto-promotion). · **✗** the existing §Composition gate already steers correctly and the miss was reader sloppiness (then drop). · **→** `character.md` §Composition (the protection test), §The two-survivals plane (locate the corner before any composite claim), the open-interval/degeneracy discipline. · *triggers:* the outside-model sleep essay (2026-06-03) — read sleep as a minted `both`; **(2026-06-13) archetypes-as-circulations** — six loaded macro-functions (persist / replicate / acquire / defend / coordinate / explore) read as hard bits when only the protection test certifies one; **(2026-06-13) sink-does-all-the-work** — the *dual* over-read, a loaded endpoint (the sink) credited with the whole dynamics. · *shape:* **apparent-structure ≠ the protected observable** (only the protection test certifies the hard bit) — now elevated to the cross-track class `shape:apparent-structure-not-observable` (see Shapes). Already recurring in-core (logged, NOT promoted): the neither-corner corollary ("soft-sector capability is silent on the hard sector"), the §Composition circularity gate, and this nudge; cf. the deformer $\\lambda{=}0$ reciprocal case ($\\mathcal{A}{=}0$) and the detailed-balanced DNA cycle ($\\mathcal{A}{\\approx}0$ pre-fuel). **Recurrence FIRED** (2026-06-13, two further in-shape misreads) → the `↑` gate's HELD-policy review is now **due**; the dual over-read sharpens the shape from "don't over-credit the figure" to **don't over-credit either pole — the gradient is the bit**.

### Shapes — cross-track recurrence classes *(navigation; nothing moved or deleted)*

A **shape** names a steering-failure (or structural) pattern that recurs across *different rungs/tracks* — the nudge holding pen, the steeping register, in-core gates — so the recurrence is legible in one place. A shape does not itself promote; it triggers the relevant track's review (e.g. a nudge's HELD review) once its instances cluster. Like the Dashboard, this is a navigation layer: every referenced entry is preserved verbatim in its own rung.

* `shape:apparent-structure-not-observable` — *apparent structure (periodicity, capability, a striking figure, a deep-looking endpoint) is never the protected bit; only the protection test certifies the hard sector.* **Dual form (2026-06-13):** don't over-credit *either* pole — neither the figure nor the sink; the **gradient** carries the bit (a sink read as doing "all the work" is the same error as a figure read as self-sustaining). *Instances:* `nudge:loaded-endpoint-descent` (sleep as a minted `both`); `predictor-soft-sector-blindness` (feedforward predictor confident-and-false on hard-sector questions); in-core neither-corner corollary (soft capability silent on the hard sector); in-core §Composition circularity gate (the protection test is the non-circular observable); **(2026-06-13)** archetypes-as-circulations (six loaded macro-functions read as hard bits); **(2026-06-13)** sink-does-all-the-work (the dual — a loaded endpoint credited with all dynamics, now its own entry `sink-extracts-the-work` with the falsifiable, non-over-read form). *Status:* recurrence fired; `nudge:loaded-endpoint-descent`'s HELD review is **due** — name the sentence, **do not auto-promote**.

### Sharpening — active work on a named owed item
* `predictor-soft-sector-blindness` **\[sharpening\]** — *verdict:* an output-trained (feedforward) predictor occupies the **neither corner** by architecture — large soft-sector capability, the hard sector structurally absent (acyclic adjacency ⇒ $\mathcal{A}\equiv0$); on a hard-sector question it is **confident-and-false** (a narrow-but-wrong CI = the metric-sector signature of *averaging across* a topological branch boundary it cannot represent — distinct from under-sampling, which more data fixes). **↑ FIRED → sharpening (2026-06-28):** the mechanism is the absent persistent **rejection operator** $(I-P)x$ — the predictor learns the interior manifold $P$ (cost ~ volume) but discards the orthogonal complement, so it never represents the codim-1 boundary where the protected branch flips; that boundary is the stable foliation the rejection-anchor work named (receipts §Rejection-anchor, `pa:oseledets-pesin`). Surfaced from AlphaFold / prediction-powered-inference (Angelopoulos–Bates–Jordan; phosphorylation = a driven NESS cycle, `pa:kinetic-proofreading`). · **↑** a substrate where soft/hard cases are labelable: the bias **concentrates on hard cases**, does **not** shrink with soft-sector data, boundary-straddling ground truth restores coverage with fewer labels. · **✗** the bias is uniform or shrinks with any added data (plain under-sampling, character adds nothing). · **→** `character.md` §The two-survivals plane; same shape as `nudge:loaded-endpoint-descent` (`shape:apparent-structure-not-observable`).  

* `seam-classification` **\[sharpening; layering crossed\]** — *verdict:* every deformation-calculus threshold is a **seam** read through singularity theory (`pa:singularity-stratification` — Thom/Arnold/Golubitsky–Schaeffer/Whitney): a (singular set, **codimension**, universal unfolding), so the rounding exponent and "hard vs soft" *are* the normal form. Instances: the composition associator = codim-0 smooth bulk; the hold-both threshold = a codim-1 transcritical the drive unfolds ($q{=}\tfrac12$); the Boolean-deviation = a codim-1 crossover of width $w(D)$ (`battery:seam-collapse`); the achiral $\mathcal{A}{=}0$ point = the singular set itself; the exceptional point = codim-2; the amplitude/topological face-boundary = resolved-independent (the EP suppresses the complex pair while $\mathcal{A}$ survives, `kill_protected_sign.py`). **Crossed:** the **metric→singularity→topology** layering — the protected sign is the **non-unfoldable stratum** (§The two tangent sectors). Discipline: the rigorous unfolding/codimension core, not catastrophe overreach. · **↑** codimension read off a real substrate → a core primitive. · **✗** a seam with no well-defined codimension; or a protected sign that unfolds smoothly with $M$ stable and $\mathcal{A}\ne0$ (a propagating kill, attempted + survived, `kill_protected_sign.py`). · **→** `character.md` §The two tangent sectors; receipts §Deformation calculus.  
* `dimensionless-substrate` **\[sharpening\]** — *verdict:* the dimensionless self-probe instanced on rock-paper-scissors; the residual is a *second* real substrate to promote the self-probe ruler. · **↑** a second real substrate hosting the dimensionless self-probe at the standard operating points → `battery`. · **✗** the dimensionless self-probe fails to close on a real frustrated substrate. · **→** the self-probe ruler; ties `battery:dimensionless-self-probe`.  
* `consolidation-ascent` **\[sharpening\]** — *verdict:* one heat-tax mechanism, three faces — an *endogenous* offline drive-withdrawal duty cycle, a compression operator contending with active maintenance for one server, and the tower ascent that *is* consolidation. Definite mechanism: only the **external-rewiring** branch survives a drive-stop, so consolidation = **converting thermalized-crossing traces (lost on reset) into structurally-stored ones** before the drive withdraws ($\mathcal{A}$'s *value* persists in the wiring while its current + soft sector relax). Survivable **only at $\beta{<}1$** (the slow tail clears the gap; $\beta{=}1$ re-mints). The **gap-burstiness is the queue-tail face of the same $\beta$** as the aging exponent — the duty cycle is **locked to the $\beta$-collapse, not a free knob**. Limit object = a **cyclic character** (`flow-resident-number`). Candidate substrate: a **sleeping nervous system**. · **↑** a runnable spec there: the coarse-grained kernel form forced-not-fitted **and** burstiness/aging/queue-tail inverting to **one $\beta$** → `battery`. · **✗** the coarse level fails the two-mode-kernel form (kills RG-closure for $\beta{<}1$); **or** a duty-cycler whose persistent bit is thermalized-crossing yet survives reset; **or** the three $\beta$-registers fail to collapse. · **→** receipts §RG closure; `staked:memory-collapse`; `flow-resident-number`; `character.md` §Branch membership.  
* `frustration-ascent` **\[sharpening\]** — *verdict:* the generative-recursion bet, resolved **conditional** — *generative* of organization/chirality/topology (these flow with the NESS affinity ⇒ mintable, legal) and *parasitic* on the drive (continuous-amplitude autonomy is supplied, not minted — receipts §Amplitude autonomy, a closed negative). The single-substrate triple obstruction (self-light + gapped + complex-pair-seedable mutually frustrate) is a **same-level** closure obstruction, **evaded by a two-level stratified cascade** (a base self-lights + selects a handedness; a gapped upper carries an autonomous protected cycle surviving drive removal). The legal three (minting / protection / sustained run-loop) shown end-to-end (calibration), confirmed on the DNA reaction network. Candidate instrument: **JCVI minimal cells** (genome reduction probes which circulations are structurally necessary). · **↑** a *real* substrate where a coarse register's chirality mints **and** the cascade closes robustly. · **✗** amplitude-autonomy minting — **closed negative**. · **→** the closure / composition; stays sharpening for the real-substrate chirality-minting instance + full robustness.  
* `self-referential-closure` **\[sharpening\]** — *verdict:* the §Status open item ("a `⊗`-fixed-point among its own drivers") is a **three-layer stack**; core has only the first. **(i) Minting** — coupling creates a bit neither part carried (HAVE; four instances). **(ii) Templating** — can a circulation bias sign(𝒜) of the bits it mints toward its own value? (value-transmission, distinct from minting's existence) — **MEASURED degenerate** (`experiments/templating_kernel.py`): a faithful, drive-independent, parity-equivariant kernel, but the von Neumann channel `I(parent;child)` **saturates at `C≈1 bit ≪ k`** (field/crystal-class, not tape-class) — physics buys ~1 bit of degenerate templating; open-ended heredity stays sub-von-Neumann-threshold without a copyable tape (receipts §Composition · *Templating*). **(iii) Self-wiring** (owed, the bigger layer) — the recursion re-enters minted characters as new nodes/edges, writing its own union graph (minting goes *endogenous* = the autopoietic core). **Heredity = templating *over* self-wiring.** Both layers **over-determined by imports** ⇒ calibration, not discovery (templating: `multitype-branching` / `frank-autocatalysis` / `quasispecies`; self-wiring: `raf-sets` / `hypercycle` / `akr-balance-dynamics`; bootstrap necessity `von-neumann-automata`, candidate `cairns-smith`). **Thermodynamic guard:** self-wiring ≠ self-driving — drive stays external (`sink-extracts-the-work`, `drive-not-primitive`). **Priority:** templating *is* the bootstrap test; self-wiring + the cascade-depth law deprioritized until it clears. · **↑** *(self-wiring)* an explicit $G_{n+1}=F(G_n,\text{minted}_n)$ producing a frustrated union graph, beyond the one synthetic instance. · **✗** *(templating)* the recursion mints sign(𝒜) **independent of the seed** (no pre-genetic heredity); *(self-wiring)* re-entry adds no frustration-changing edges (ordinary cascade, not autopoietic). · **→** `character.md` §Cascade / the self-referential limit, §The minting claim, §The morphospace; `project_syn3_alive_loop_northstar`. **Sibling of** `frustration-ascent`, `consolidation-ascent`, `selective-coupling-class`.  
* `wall-as-type-boundary` **\[sharpening\]** — *verdict:* the marginal point is a type-boundary **ladder** of plateaus (normally hyperbolic invariant manifolds carrying the universal normal form) joined by three boundary kinds — ignition (constructive landing), topological flip (free sign-face transition), closure-loss ($\varepsilon\to1$, loss of normal hyperbolicity); only the last is wall-like. Plateau side instanced on the coupled Stuart–Landau cascade (CLV minimum angle bounded); closure-loss via Benjamin–Feir ($\theta_{\min}\to0$, $\lambda_{\max}{>}0$); the $N{=}3$ cascade reaches the boundary *without* chaosing (loss-of-hyperbolicity ≠ forced chaos). · **↑** a real cascade: levels = plateaus, transitions = the named boundaries, a level at $\varepsilon\to1$ shows $\theta_{\min}\to0$ → promotes the ladder. · **✗** the CLV discriminator fails to separate plateau from boundary, or a real level is not such a plateau. · **→** coarse-graining (marginal point as loss of normal hyperbolicity); receipts §Marginal point. Canonical delay-driven mechanism + both sides on a real cascade still owed.  
* `chirality-protection` **\[sharpening\]** — *verdict:* the protected object in a minted circulation is the gauge-irremovable affinity / discrete graph-flux sign — **not** the exceptional pair (suppressible) and **not** an integer charge (holonomy sub-integer); run synthetically (sign 0/200 graph-fixed deformations, EP killed 53/200, holonomy $\le0.16$); the binding discharged by import (calibration). · **↑** a *second, real-substrate* instance of the affinity/exceptional-pair separation. · **✗** a graph-fixed smooth deformation reversing the sign without rewiring; or a substrate where overdamping the EP also erases the affinity. · **→** receipts §Chirality protection, §Topological-drain; `battery:sign-interior`.  
* `cascade-ledger-split` **\[sharpening\]** — *verdict:* the conjugate cascade's two ledgers are one coarse-graining EP decomposition — $\langle\sigma\rangle_{\text{tot}}=\langle\sigma\rangle_{\text{res}}+\langle\sigma\rangle_{\text{hid}}$, the hidden part the integrated-out fast circulation's dissipation, aligned with the metric/protected split by Schur (`pa:transverse-decomposition`); closes by composing standing imports (heat-tax tower + transverse decomposition + `pa:esposito-coarse-graining`/`pa:timescale-ep`), analytical-only. · **↑** a real two-level substrate measuring the three EPs — they sum, hidden part in the protected sector → `battery`. · **✗** a two-level substrate where they fail to sum, or the hidden EP sits in the metric sector. · **→** core §The conjugate cascade; receipts §Conjugate-cascade ledger.  
* `qec-transverse-decomposition` **\[sharpening\]** — *verdict:* the surface-code reading of the transverse decomposition, **computed**. In the $[[9,1,3]]$/$[[25,1,5]]$ rotated surface code the logical sector carries zero syndrome (symplectically transverse), the protected bit is written at unit cost and flipped only at the code distance ($P_L\propto p^{(d+1)/2}$), and the bit-flip barrier exponent is **exactly invariant** to a syndrome-active Z current (CSS X/Z independence = the transverse separation), leaking $\propto\delta$ only under Y-mixing. **Boundary:** a *same-sector* current is never exactly transverse, so exact $\Delta V\perp\mathcal{A}$ is **symmetry-protected, not generic** (same boundary as the triad's $Z_3$). Calibration, but it unifies three substrates (triad/surface-code/cascade) under Schur. **Now a portable protocol** (the TDT, handoff §TDT) — run on the **aging exponent** (biased ring): invariant to the protected current to machine precision, coupling on only when the ring's $Z_N$ breaks (receipts §Glass aging transverse) ⇒ the current is barred from *all* metric observables. Scope: the selection-rule *threshold* (zero coupling iff the symmetry holds) is substrate-invariant; the *onset power* is not (QEC linear, glass $\propto\delta^2$). · **↑** a *real* substrate showing barrier-invariance to a symmetry-separated current + the leak under mixing → `battery`. · **✗** a CSS code whose logical-barrier exponent shifts under a pure conjugate-sector current; or a within-sector current shown exactly transverse with no protecting symmetry. · **→** `character.md` §The two tangent sectors + §The two-survivals plane; receipts §QEC transverse decomposition; `pa:transverse-decomposition`.  
* `selective-coupling-class` **\[sharpening\]** — *verdict:* a coupling **class** beyond the core's two (metric / frustrated) — **selective coupling**: hard-sector-rich, soft-sector-poor (passes circulation, suppresses population mixing; $\tau_{\text{mix}}\gg\tau_{\text{circ}}$). **Two tiers:** Tier 1 — symmetry-exact ($V_{AB}=0$ by symmetry; the DFS/Schur/transverse split — *already core*, §The two tangent sectors); Tier 2 — smallness-suppressed ($|V_{AB}|\ll|\Gamma_{AB}|$, a *regime* not a symmetry — the new content, which I5 forbids masquerading as protection). Coined the **stratification number** $\mathcal{S}\equiv|\Gamma_{AB}|/|V_{AB}|$. Forced sub-results (union holonomy $\mathcal{A}_{\text{union}}=\oint\Gamma_{AB}\,dx$; skew-$\Gamma$ conduit; minting stratum-agnostic) **promoted to core 2026-06-18** (§The minting claim, §The space of characters, §Motion and proximity; receipts §Composition). Un-forced **payload = a cascade-depth law:** the Tier-2 leak compounds level-to-level → $N_{\text{levels}}\sim f(\mathcal{S})$, the one thing Tier 2 buys. **Theorem-form:** with $M=$ (minted)/(coalesced) structure, a cascade is indefinitely extensible ($\varepsilon<1$ sustained) iff $M>1$, $N$ diverging as $M\to1^+$; $M=1$ plausibly *is* the marginal point — unifying $M$, $\mathcal{S}$, $\varepsilon$. **Observer corollary:** observer ⇒ $\beta<1$ ⇒ deep cascade ⇒ ascendant couplings (every arrow but the last a framework result; the necessity-arrow unearned, sole open competitor = active maintenance/repair). · **↑** a tunable-$\mathcal{S}$ substrate ($\ge3$ strata): cross-stratum minting localized in $\oint\Gamma\,dx$ (sign drive-locked), the metric channel parametrically suppressed, $N_{\text{levels}}(\mathcal{S})$ forced-not-fitted → `battery`. · **✗** (a) no selectivity outside Tier-1 symmetry; (b) collapse depth independent of $\mathcal{S}$; (c) a stratified stack whose $\sigma_{\text{hidden}}$ sits in the *metric* sector (fires `cascade-ledger-split`'s kill). · **→** `character.md` §The space of characters, §Composition, §The two tangent sectors, §Coarse-graining and the marginal point. **Absorbs** the retired `cross-stratum-transduction` (verdict in `character_substrate_ledger.md`); **sibling of** `sink-extracts-the-work`, `cascade-ledger-split`; **distinct from** `cascade-climbing-universality`.  
* `cascade-climbing-universality` **\[sharpening\]** — *verdict:* the bet that **cascade-climbing has a universality class** — the tower ascent as a **stochastic growth interface**, level-to-level roughening governed by universal exponents + a universal fluctuation distribution. Candidate (import, conjectured): **KPZ** — the lateral propagation `selective-coupling-class` records is the candidate KPZ nonlinearity $(\lambda/2)(\nabla h)^2$, the self-similar coiling the candidate KPZ scaling. Discriminator = **driven vs relaxational at the scaling level**: KPZ ($\beta{=}1/3$, $z{=}3/2$ in 1+1D; **Tracy–Widom**) is driven, Edwards–Wilkinson ($\beta{=}1/4$, $z{=}2$; **Gaussian**) equilibrium — so the bridge is **KPZ-class $\Leftrightarrow\mathcal{A}\ne0$**, EW $\Leftrightarrow\mathcal{A}{=}0$ (the fluctuation distribution the robust readout). If it holds, growth statistics are a soft-sector readout of the hard-sector affinity (the TDT logic at the level of a scaling class). · **↑** a real cascade as a height field: fluctuations in **one** class **and** the class **tracks $\mathcal{A}$** → `battery`. · **✗** mean-field/Gaussian or no-known-class fluctuations; or a driven cascade robustly EW (or a balanced one KPZ); or the height-field reading not faithful. · **→** `selective-coupling-class`, `consolidation-ascent`, `wall-as-type-boundary`/`seam-classification`; `character.md` §Coarse-graining and the marginal point; import flagged for `character_prior_art.md` (KPZ/EW/TASEP).  
* `sink-extracts-the-work` **\[sharpening\]** — *verdict:* the source/sink/conduit decomposition with the **sink first-class, co-equal with topology**: protected topology is **paid upfront** then inert ("a sign is free to maintain"), the **conduit** is a fungible metric carrier (the puppet), the **work is extracted at the sink** (the cold reservoir turns stored topology into a running circulation), and $a{=}\ln(G_0/L)$ is two-sided — the *gradient*, not either pole, does the work. (Refined by `drive-not-primitive`: a *dipole* on the reservoir axis, a *monopole* on the metric↔topological axis.) Re-weights the core's "sink = invoice": at $a{=}0$ no work is convertible and character dissolves **no matter how large $G_0$**. Candidate instances: the **Brownian gyrator** (vary the cold bath alone, $\mathcal{A}$ moves ⇒ sink generative not bookkeeping); a **trained network** (weights = upfront topology, generation = the kinetic release at the sink). · **↑** the **sink-sweep** on a real substrate: hold source, vary sink, $\mathcal{A}$ moves → `battery`. · **✗** varying only the source reproduces the same $\mathcal{A}(a)$ (sink is passive bookkeeping); or protected content in the conduit not reducible to {topology}∘{sink-work}. · *caution:* "only the sink is real" is the mirror over-read (`shape:apparent-structure-not-observable`). · **→** `character.md` §The conjugate cascade, §Isolation; **sibling of** `cascade-ledger-split` (two-level vs this single-level asymmetry).  
* `drive-not-primitive` **\[sharpening\]** — *verdict:* "drive" names no agent — it is the **geometry of the reservoir coupling** the system sits on (a held difference / slope), supplied externally and **indispensable** (cut it and the geometry is a *fossil* — frozen morphology, no σ/𝒜/response — not purer character). What the system **owns** is a net one-signed topological charge — the **character-monopole** $\mathcal{A}=\oint v/D$, the charge of a *circulation* (curl-source; gauge-irremovable Harary-unbalance), around which the current winds; the drive sets only the **spin rate** ($J_{ss}$ flows with $a$, sign/structure drive-independent — the §Amplitude-autonomy result read from the geometry side, a collapse). **Corrects the over-symmetric gloss:** the source↔sink axis is a *dipole* (co-equal reservoirs); the metric↔topological axis is a *monopole* (owned charge vs fungible balanced carrier). **Polarity guard:** the literal $\nabla V$ gradient is a *divergence*-monopole (the soft sector); the object meant is the *curl*-monopole $\mathcal{A}$. **Scale-bound character:** the RG climb discards geometry and keeps the invariant, so the bit's scale-invariance is the *symptom of having projected the geometry away* — character is **scale-bound**, only its protected reading scale-free; the cascade transduces the **reading, not the character**. · **↑** a substrate where coarse (topology-only) and fine (geometric) descriptions are operationally separated by a perturbable response the invariant can't predict → `battery`; or $\mathcal{A}=\oint v/D$ made a forced quantitative statement on a real vortex-bearing substrate. · **✗** the topological invariant **suffices** (every response recoverable from the coarse bit, geometry irrelevant — then "drive" can be a primitive); or a sustained character on a *single* reservoir. · *caution:* "there is no drive" overshoots into the frozen-geometry trap — the disciplined form is **owned charge, borrowed spin, and the spin is not optional**. · **→** `character.md` §The conjugate cascade, §Two degrees of freedom, §Branch membership; **sharpens** `sink-extracts-the-work` + `selective-coupling-class`.  
* `universal-invariant-classification` **\[sharpening\]** — *verdict:* the program's make-or-break. Is $\operatorname{sign}(\mathcal{A})$ **the universal invariant of a single classifying structure**, or a per-substrate quantity sharing a name? Three outbound passes (`docs/universal_invariant returns.md`) return a **clean negative on the import** — no model-independent classifying structure; the literature's is *substrate-bound* (Polettini gauge-cohomology `pa:gauge-thermodynamics`; Harary/Zaslavsky `pa:signed-balance`; Andrieux–Gaspard `pa:cycle-affinity`). The deformation framing is **obstructed** (a 3-state cycle and a colloid ring both carry $\operatorname{sign}\mathcal{A}{=}{+}1$ in different state spaces — no shared classifying *space*). **Survives (consolidation win):** the *construction* — gauge-irremovable sign of a cycle holonomy — is substrate-independent + named; the bit is $\mathbb{Z}_2$, sub-integer, on state space. **The residue:** universality, if real, is a **composition law**, not a shared space. **Composition build resolved** (`composition_law.py`): all mechanical conditions pass **but as calibration** — it is the **cycle-space homology import**, and it *splits* (the existence/frustration bit composes as a linear cycle-space algebra; the value bit $\operatorname{sign}\mathcal{A}$ is $\mathbb{R}$-additive / coupling-dependent, no group fusion). **Neither ↑ nor ✗ fires clean** → the **C-middle**: metaphor-collapse averted (a 2nd computed cross-substrate law beside the transverse theorem), physical-category not won. The last open piece (the colloid superselection premise) is **RETIRED 2026-06-26 as parity-trivial** — a symmetry-forced $c_1{=}0$ tests the rig, not the framework. · **↑** the composition law shown a genuine substrate-independent monoidal/fusion structure → character is a physical category, crosses. · **✗** the composition is substrate-specific → the metaphor-verdict earned. · **→** core §Composition / §The minting claim, §The two tangent sectors; receipts §Universal-invariant classification, §Composition-law build, §The binding; **extends** `seam-classification`.  
* `escape-degenerate-replication` **\[sharpening\]** — *verdict:* the pre-life threshold reframed in the framework's terms; the value is the **problem-formulation**, not (yet) a result. Replication is **cheap** — *degenerate pattern-replication* (Gray–Scott spots, vortex stacks: copying a fixed form, ~1 bit, no tape; von Neumann's **trivial** reproduction) — so the question is **how to escape** it. Ladder: a single dissipative structure → a **dissipative medium** (compartmentalization/transport/persistence/variation for free, all **metric**-sector — a rich *stage*) → an **ecology** (relational coexistence; real non-anthropic bound information; the bits-rich, bit-less neither-corner). Threshold: the ecology becomes a **genuine environment** ("a place to be *fit* in") the instant something **mints a protected bit that reads it**. So **environment = information × reader** — *relational, not anthropic*; the reader is any minted circulation; *fitness* is the bit×environment relation. The coexistence graph does double duty (selective environment **and** minting graph — `selective-coupling-class`). Viable window = the selective-coupling regime (mixing ≫ circulation; the compartments protect the bit). **Falsifiable scale prediction:** gravity → stratification → a vortex/cell scale (Batchelor < L < Ozmidov $L_O{=}(\varepsilon/N^3)^{1/2}$); if that window coincides with the minimal frustrated-loop scale, the cascade has selected the *scale of character* (operationalized by `battery:strata-current`). **Carbon is gravity-contingent (corollary):** two filters — the gravity-set strata filter ∩ the tape filter — co-select which chemistry participates; carbon clears both on Earth, not by necessity. Casting, not minting. · **↑** a real/simulated dissipative medium shown to mint a **transmissible** (degeneracy-breaking) protected bit → crosses toward the bootstrap. · **✗** every medium's minted differences stay degenerate (the window empty or ~1-bit-capped) — no escape without an imported tape. · **→** `self-referential-closure` (the bootstrap), `selective-coupling-class`, `character_substrate_method.md`; imports `pa:von-neumann-automata`, `pa:cairns-smith`, `pa:raf-sets`/`pa:hypercycle`. **Status:** a sharper *question*; current best is the templating result (degenerate, ~1 bit, needs a tape). Durable yield = character as a **constraint-language / problem-formulator**.  
* `reading-transition` **\[sharpening\]** — *verdict:* the abiogenesis arc's load-bearing object ("define reading before heredity — the framework lives or dies there"). A **reader** = a protected circulation whose behavior is steered by ecological distinctions — the one *novel* pre-biotic object (compartments/gradients/replication/selection all have mature theories; the reader does not). **Reading splits like reproduction:** **cheap (metric)** — an ecological distinction modulates the loop's rate/branch/occupancy (the cross-rule; instantaneous, genuinely pre-heredity); vs **hard (bit-level)** — the ecology reaching the protected sign, which superselection **bars instantaneously** and permits only by **rewiring = selection = proto-heredity**. So reader and heredity **co-emerge at the bit**; the tape is a further *unforced* von-Neumann step. **Order parameter over a LINEAGE:** the two readings mirror the two reproductions (metric:state :: degenerate:pattern; bit:sign :: heritable:bit), and **selection is the *unique* channel metric→topological** (a projection operator, no instantaneous path). So $R_{\text{hard}}=\partial\langle\operatorname{sign}\mathcal{A}\rangle/\partial(\text{ecology})$ **across replacement** = 0 below threshold (a kingdom of **metric readers**: $\partial M/\partial E\ne0$, $\partial\mathcal{A}/\partial E{=}0$ in a lifetime), $\ne0$ above. The transition = **the appearance of ecological access to the protected sector** (same bridge, opposite projections). **Measured** (`battery:readability`): the metric/bit split, the machine-exact within-lifetime superselection null, $R_{\text{hard}}(\delta)$ $\sim$linear at the symmetry break. · **↑** a real/simulated protected loop whose metric behavior tracks a tunable ecology **and** whose bit reads only under rewiring ($R_{\text{hard}}\ne0$ over a lineage) → promotes the bootstrap. · **✗** reading inseparable from heredity even at the metric level (superselection violated); or metric reading never reaches the bit even under rewiring. · **→** `escape-degenerate-replication`, `self-referential-closure`, the cross-rule + `pa:transverse-decomposition`, `battery:readability`; `docs/character_abiogenesis.md`.  

* `circulation-held-capacity` **\[sharpening\]** -- *verdict:* the decomposition *Organization = circulation-held $K(C)$ + archive-held*; $K(C)$ = the organization a NESS recovers after **complete part-turnover**, archive-held = distinctions whose fidelity exceeds the circulation's self-specifying capacity (DNA on this reading). Non-circular: $K(C)=K_{\text{topo}}(b_1)+K_{\text{metric}}$ (the turnover-stable slow-manifold dim beyond the fixed point; limit-cycle phase $=1$). **Anchors, forced-not-fitted:** KaiABC $K=2>b_1$ (`battery:circulation-capacity`); PO reaction $K=4$ ($b_1=3$, `bfso_capacity.py`); + QEC keep-as-capacity (`qec_keep_capacity.py`, receipts §QEC keep-as-capacity): keep $=b_1=k$ **literal** ($k=2-\chi$, toric $k=2$), distance-vs-keep split **exact**, wall = channel capacity (hashing + threshold $p_{\text{th}}\approx0.109$, DKLP 2002). **Archive-term MEASURED on Syn3A** (`syn3a_archive_term.py`): the cut **blurs at the reader** (≈41% of archive bits = GIP) — the two-term sum disfavored at high archive (overlap at the self-referential seam); a sharpening, not a tidy ↑ (could have come out blur≈0). **PROMOTED TO CORE 2026-06-25 — §The keep and the archive** (decomposition + $K(C)$ + anchors + the wall + archive-term credited to KW/Rosen/Pattee + the Syn3A blur); frontier holds the residue. · **↑** a **second** point, same modality, $K_{\text{metric}}$ forced-not-fitted (cardiac SA node / CDK-APC / glycolytic nominated) — but → core now gated on four uncleared blockers: (i) archive-held term untested where circulation *fails* (both anchors at archive≈0); (ii) $K_{\text{metric}}$ always 1 (no forced $\ge2$); (iii) generality within one biochemical class (inorganic Oregonator owed); (iv) $K_{\text{topo}}$ not uniformly defined (affinity-sign vs cycle-count). · **✗** $K=b_1$ on every substrate (nothing turnover-stable beyond the topological count); **or** the cut cannot be drawn choice-independently on a harder case. · **→** `character.md` §The keep and the archive; receipts §Real-substrate instances, §QEC keep-as-capacity. **Sibling of** `escape-degenerate-replication`, `self-referential-closure`, `drive-not-primitive`, `qec-transverse-decomposition`.
* `engine-character-onset` **\[sharpening\]** — *verdict:* the engine→character crossing (a metric energy-capture engine minting its first protected bit) is a **topological onset, not a bifurcation** — **settled by import.** $\mathcal{A}$ is a function on the cycle space $H_1$ (Schnakenberg cycle affinity; Polettini gauge-thermodynamics: $\operatorname{sign}\mathcal{A}$ = the gauge-irremovable Wilson-loop sign, a state-space $\mathbb{Z}_2$ invariant): identically zero/gauge-removable until a driven frustrated cycle closes, gauge-irremovable after. So character is **minted discontinuously at the rewiring**, not ramped on by harder driving (already core §Minting; a synthetic sweep is a demonstration not a test — `strata_sort.py`, $\mathcal{A}{=}\Omega$, algebraically pinned). The import settles the *form*, not the *occurrence* (a real primordial engine crossing = the abiogenesis bootstrap, no homology establishes it). **↑ TRANSITION-KIND CROSSED (2026-06-25):** the carrier-depth reconciliation done, core sentence landed — §Motion and proximity states the onset's kind (continuous drive-set bifurcation at $N{=}2$ / discontinuous gauge-irremovable rewiring at $N\ge3$), receipt §Onset kind. **Only the *occurrence* remains open** (folds into `escape-degenerate-replication` / the §Status bootstrap). · **↑** a real primordial engine shown to cross (= the abiogenesis bootstrap; no cycle homology establishes it). · **✗** a genuine driven frustrated cycle ($b_1\ge1$) whose $\operatorname{sign}\mathcal{A}$ is tunable through zero by a smooth non-rewiring deformation (breaks the homology import), or the transition shown generically continuous. · **→** `character.md` §Minting / §The object. **Sibling of** `escape-degenerate-replication`, `reading-transition`, `circulation-held-capacity`, `drive-not-primitive`. `pa:cycle-affinity`, `pa:gauge-thermodynamics`.  
* `reader-by-overreach` **\[sharpening\]** — *verdict:* a candidate **dynamical path** from the keep's ceiling to the **reader** (the owed `self-referential-closure` self-wiring piece; the mechanism for `reading-transition`'s "selection = the unique metric→topological channel"). **Mechanism:** near the wall a keep that **overreaches** (closes loops more heterogeneous than its turnover re-specs, ~`N log N` vs ~`N`) fails and deposits a **metric trace**; once consulting the trace costs less dissipation than re-minting the loop **including the reader's own upkeep**, readout machinery is thermodynamically selected ⇒ the reader = the **metric→topological back-channel** (why the reader is both-sectors — the Syn3A seam). **Offline-read corollary:** reconstruction time-shares into offline phases; falsifiable signature **coordination ⊥ reading**. **Deferral audit** (`docs/research/research_prompt_reader_by_overreach.md`, 3 models): the concept is **owned in pieces, deferred** (crossover = Still / England / Bennett / KW; trace = Keim RMP 2019; alternation = SHY / Hopfield–CLS; form = Eigen / Pattee / Crick–Gould) — none assembling the reader-selection. **Residue (small, kept) = two integrations + one prediction:** (1) the crossover anchored to the keep's `N log N`-vs-`N` wall as the reader-selection pressure; (2) the sector reading predicting Syn3A's reader-dominated archive (≈41% of bits); (+) the generalized **coordination⊥reading** anti-correlation. **Disconfirmer #3 MEASURED** (`syn3a_reader_upkeep.py`): reader-upkeep ≈46% of the proteome budget, co-equal with circulation (not dominant) — the cell pays it and still reads ⇒ re-mint ≥ read+upkeep, a steep-wall witness, **#3 not triggered** (upkeep side only; the re-mint counterfactual / `N log N` wall stays owed). **Accuracy-is-fitness sharpening (steeping):** accuracy = the keep's fitness function (Eigen sans replication); the archive is minted at topological-active correction (redundancy for an unmeasurable bit); upkeep = the fidelity price (TUR). · **↑** the reader-upkeep crossover + trace re-readability measured on a *real* substrate (not rigged). · **✗** overreach traces generically uninformative; or a NESS that re-specs fully online (no offline phase); or upkeep makes consultation cost more than re-minting in all regimes. · **✗ gate** a synthetic crossover sweep is a demonstration, not a test (`feedback_no_synthetic_sidequests`) — **do not build it.** · **→** `self-referential-closure`, `reading-transition`, `circulation-held-capacity` / core §The wall, `consolidation-ascent`.
### Battery — executable falsifier specs

* `battery:seam-collapse` **\[battery\]** — *verdict:* the amplitude/drive-seam scaling-collapse battery; runnable on synthetic now, the forcing needs a real substrate with a measurable FDR. Target invariant $(\\alpha, F)$, not a coefficient series. · **↑** stable $\\alpha$ \+ universal collapse \+ substrate-family profile classes on a real substrate → promotes `seam-classification` (the thm9 crossover-width instance). · **✗** width saturates as $D\\to\\infty$; or no single $\\alpha$; or equal $(\\alpha,w)$ with incompatible profiles. · **→** receipts §Deformation calculus.  
* `battery:sign-interior` **\[battery\]** — *verdict:* the protected-sign interior-circulation kill — interior measurement only (never the never-attained boundary), reading the binary sign of the affinity, not a graded triality. Vindicated on rock-paper-scissors (a real substrate carries a protected sign on a directed 3-cycle, drive-independent, flipping only by rewiring; the symmetric null carries none). · **↑** a protected sign on a real substrate always sits on a frustrated triad → promotes the central commitment *(fired — crossed)*. · **✗** a real substrate with a protected sign and no triad; or the synthetic sign flips under finite drive variation without rewiring. · **→** the protected current; receipts §Central commitment, §Two bits.  
* `battery:wall-ladder` **\[battery\]** — *verdict:* the plateau-ladder falsifier — a cascade's levels are normally hyperbolic plateaus separated by the three boundary kinds; the discriminator is the CLV minimum angle $\\theta\_{\\min}$ \+ the smallest transverse exponent. Both faces demonstrated: the plateau side on the coupled Stuart–Landau cascade ($\\theta\_{\\min}$ bounded, false-positive controlled), the closure-loss side via a Benjamin–Feir route ($\\theta\_{\\min}\\to0$, $\\lambda\_{\\max}\>0$). · **↑** on a real cascade: levels read as plateaus, a level at $\\varepsilon\\to1$ shows $\\theta\_{\\min}\\to0$ → promotes `wall-as-type-boundary`. · **✗** the discriminator fails, or a real cascade level is not such a plateau. · **→** `wall-as-type-boundary`; coarse-graining. The canonical delay-driven mechanism stays the deferred test.  
* `battery:dimensionless-self-probe` **\[battery\]** — *verdict:* the dimensionless self-probe ($\\mathcal{T}\\ge1$)  
  + the canonical affinity, instanced on rock-paper-scissors (the empirical winding affinity closes onto the forced form to 1.7%, and the affinity and $\\omega/\\gamma$ are noise-independent). Scope: the noise-independence is the symmetric-multiplicative-drive case (a thermodynamic-force drive would make the affinity drive-dependent). · **↑** a second real substrate with the same noise-independent affinity → promotes the self-probe ruler. · **✗** the affinity or $\\omega/\\gamma$ noise-dependent on a real substrate, or the self-probe fails to close. · **→** the self-probe ruler; promotes `dimensionless-substrate`.  
* `battery:scale-covariant-circulation` **\[battery\]** — *verdict:* the topological bit on the scale axis — the same circulation read at two scales splits into a scale-**invariant** affinity (the bit) and a scale-**covariant** magnitude. Instanced: a legitimate adiabatic projection leaves the affinity invariant in sign and value while the current renormalizes exactly by the eliminated-mode timescale; the illegitimate maps (driven fast subspace; loop contraction) are the pre-excluded failures. · **↑** the split shown on a continuous affinity-bearing substrate → promotes the scale-relativity claim. · **✗** under a legitimate map the affinity flips or erases. · **→** scale-relativity; coarse-graining.  
* `battery:two-survivals-plane` **\[battery\]** — *verdict:* branch survival ($\\Delta V$, the basin-escape quasipotential) and current survival ($I(0)$, set by the cycle affinity $\\mathcal{A}$) are **independent axes**; substrates fill all four cells, so "no protected current" is not "no dynamical landscape." Already core: **current-only** (structurally stored — bare RPS, fuel-driven DNA: $\\mathcal{A}\\ne0$, mirror reachable only by rewiring) and **both** (spontaneously selected — homochiral: $\\mathcal{A}\\approx21.8$, $\\Delta V\\approx0.018$; **second instance** the co-handed twin-cycle via exchange/$S\_2$ SSB, not parity). New corners: **branch-only** (metastable — a symmetric Hopfield attractor net gives $\\Delta V\\approx0.97$ with $\\langle\\sigma\\rangle=\\mathcal{A}=0$ to machine precision, real Jacobian; `experiments/hopfield_corner.py`), the mirror of bare RPS; **neither** (soft-metric — a 2-layer feedforward net on a Gaussian mixture: measured capability $I(\\hat Y;Y)\\approx2.9$ bits → the Bayes ceiling, the hard sector **structurally** absent — node-adjacency nilpotent ($\\mathcal{A}\\equiv0$ by acyclicity), $\\Delta V$ undefined for want of recurrence; `experiments/neither_corner.py`), the first *non-trivial* substrate with no hard bit. Column boundary \= **frustration** ($\\mathcal{A}:0\\to\\ne0$); row boundary \= a **coexisting mirror** (rewiring → thermal crossing). Corollary: soft-sector capability is silent on the hard sector — the most *informative* corner carries no protected current at all. · **↑** *(crossed)* — all four corners instanced → core §The two-survivals plane \+ receipts §Two-survivals plane. · **✗** a symmetric/gradient attractor net with $\\mathcal{A}\\ne0$; or a feedforward/acyclic substrate carrying a sustained protected current; or branch survival and current survival shown *not* independent on a real substrate. · **→** core §Branch membership \+ the fidelity–protection split ("three layers, not one"). `pa:cycle-affinity`.
* `battery:chiral-rotor-triad` **\[battery\]** — *verdict:* the **minimal mechanical instance** of the protected-sign claim — three identical chiral rotors coupled in a driven ring at the tangent orientation $\\Delta\\varphi=2\\pi/3$, the circulating NESS current's sign locked to the shape-chirality pseudoscalar $\\chi=\\varepsilon\_2\\varepsilon\_3\\sin\\delta$. **Run synthetically (calibration); all three armed kill-switches PASS:** (1) the single-body propeller coupling $C\\approx3.4\\,\\chi$ by a validated regularized-Stokeslet BEM isolated by the **parity-odd projection** (so any grid-handedness / achiral artifact, being $\\delta$-even, cancels), **nonzero** by the selection rule $2\\otimes3\\supset1$ at **SNR $\\approx450$** above the floor, the bare $6\\times6$ mobility reciprocal (Lorentz) to $1.6\\times10^{-13}$; (2) the sign is **parity-locked, not drive-locked** — set by $\\chi$ at fixed drive sense, flipped only by $\\delta\\to-\\delta$ through the achiral point, with **no drive-*amplitude* escape** (honest: $J\\propto\\chi\\Omega$ is odd in the drive *arrow* — reversing $\\Omega$ reverses $J$ — but that is time-reversal, not a metric tuning); (3) the shape is **genuinely chiral** — a continuous chirality measure $0.75$ vs $7.8\\times10^{-16}$ at $\\delta\=0$. The activation envelope $F(\\Delta\\varphi)$ vanishes at the mirror orientations $0,\\pi$ and is nonzero at the ring's $2\\pi/3$ ($F\_e\\approx0.87$; pair mobility reciprocal there to $5\\times10^{-16}$). **The mechanism, corrected (carry it — do not re-import the wheel):** the *deterministic* phase-reduced (non-reciprocal Kuramoto) triad does **not** circulate — it **phase-locks**; the protected current is **noise-activated stochastic hopping** among the $\\{A,B,C\\}$-leads arrangements (a May–Leonard cyclic-dominance loop, no stable fixed point); the non-reciprocity is **drive-generated** (the even-in-$(\\theta\_2-\\theta\_1)$ phase coupling $\\propto C\\chi\\Omega F(\\Delta\\varphi)$), **not** bare-mobility; the current lives in the **collective configuration** (which body leads), not the individual co-rotating bodies. And **frustration is minimal** — the harmonic grammar / Clebsch–Gordan was a *sufficient demonstration, not the essential requirement* (a hole with a protrusion suffices; `feedback_frustration_is_minimal`). · **↑** a **built physical** chiral-rotor triad (colloidal / printed-ratchet / acoustically- or magnetically-driven) carrying the same parity-locked sign → feeds `chirality-protection`'s owed **real second instance**, crosses the line. · **✗** $C\=0$; or the sign flips under drive-amplitude tuning without rewiring; or a residual improper symmetry. · **→** `chirality-protection` (the owed physical instance), `battery:sign-interior`; receipts §Chirality protection; `pa:cycle-affinity`. **Synthetic \= calibration, never vindication** (`project_killshot_synthetic_is_imported_math`) — imported fluid mechanics read through the protected-sign lens; **does not discharge** the owed real instance. Build: `H:\triskele` (`github.com/ronviers/character-triskele`; live builder dashboard `ronviers.github.io/character-triskele`). *Crossed from sharpening 2026-06-21.*
* `battery:strata-current` **\[battery\]** — *verdict:* the **sort-vs-mint discriminator** on a driven stratified medium — does "things falling out of strata" (the metric scale-selection engineers exploit everywhere — centrifugation, fluidized beds, granular segregation, distillation trays, sedimentation) ever cross into **minting a protected current**? Two independent axes, swept (`experiments/strata_sort.py`, 3-cell ring): **stratification strength** s (the metric drive, a curl-free gradient) and **cyclic wiring asymmetry** Ω (the topological wiring). Result: sortedness rises with s (0→0.67) while the cycle affinity 𝒜≡0 to machine zero for **every** s — *no amount of separation mints a current*; a protected 𝒜≠0 requires frustrated wiring, and once wired its sign is **sort-independent** (constant across the whole s-sweep), flipping only on rewiring (Ω→−Ω). Bonus: strong sorting **quenches the current magnitude** (\|J\|: 0.23→0 as s→8) while the protected sign is untouched — the fidelity/protection (magnitude/sign) split read on the stratification axis. **Data source (the engineering insight):** the separation corpus is *salience, not affordance* — abundant, measured, real, and (the sweep confirms) all on the sorting axis (𝒜=0), so it **✗-harvests a whole class as stage-not-actor with free data**; the ↑-scan is the rare exception. · **↑** a real driven stratified / segregating medium (a fluidized bed, a granular rock-paper-scissors, an oscillatory-segregation instability) whose inter-strata transport carries a **sort-independent** circulation (sign fixed by geometry, drive-magnitude-invariant, flips only on rewiring) **and** whose characteristic scale lands in the Batchelor–Ozmidov window — a protected bit minted on a real medium at the predicted *scale of character* → promotes `escape-degenerate-replication` to a real-substrate instance. · **✗** every driven stratified medium swept is pure sorting (gradient transport, 𝒜=0, or a drive-locked wheel whose sign tracks the drive) — the separation class is metric to the end (the expected, strong negative: a salient class ruled out with real data). · **→** `escape-degenerate-replication` (the scale-of-character prediction it operationalizes), `selective-coupling-class` (the mixing-vs-circulation window), `character_substrate_method.md` (salience ≠ affordance — the gate this battery automates); `pa:cycle-affinity`. *Built 2026-06-21 (`experiments/strata_sort.py`); the scan over engineering separation data is the cheap real-data run owed.*
* `battery:readability` **\[battery\]** — *verdict:* the order parameter for the **reading transition**, **built and run** (`experiments/readability.py`; synthetic = calibration). Couple a protected loop ($\\mathcal{A}=b\\,\\Omega_0$) to a tunable **ecological field** $d$ entered as a **gradient** (curl-free metric potential); sweep $d$. **(1) Metric reading turns on generically** within a lifetime ($\\partial\\text{occ}/\\partial d=+0.21$, $\\partial|J|/\\partial d=-0.02$ — ecology reshapes throughput/occupancy: the cross-rule, pre-heredity). **(2) Bit reading within a lifetime is barred** — $\\mathcal{A}$ invariant to machine precision (spread $6.7\\times10^{-16}$; the gradient telescopes to $0$), so $\\partial(\\operatorname{sign}\\mathcal{A})/\\partial d=0$: the **superselection null** (the transverse theorem forbids the instantaneous route). **(3) The order parameter lives over a LINEAGE:** $R_{\\text{hard}}=\\partial\\langle\\operatorname{sign}\\mathcal{A}\\rangle/\\partial d$ at a selection–replacement fixed point (fitness $=$ metric work-harvest $\\exp(\\delta\\,J(b;d)\\,d)$; $\\delta=$ the symmetry-break/overlap knob) is **0 at $\\delta=0$** ($\\langle b\\rangle$ flat — ecology modulates the metric only) and **$\\neq0$ for $\\delta>0$** ($\\langle b\\rangle$ tracks $d$, $\\pm0.93$); $R_{\\text{hard}}(\\delta)$ turns on continuously at $\\delta=0$, $\\sim$linear near threshold (zero-iff-symmetric, TDT move 3). **(4) Co-emergence:** over the four corners (replacement on/off $\\times$ symmetry-break on/off) $R_{\\text{hard}}\\neq0$ **only when both** channels are open — hard reading and (proto-)heredity are the same bridge. So the bit becomes readable **only across replacement** — selection is the unique channel from the metric into the topological sector (a projection operator). · **↑** the same metric/bit split + nonzero $R_{\\text{hard}}$ on a **real** dissipative medium — the reader instanced as a measured object → promotes `reading-transition`, `escape-degenerate-replication`. · **✗** bit readability nonzero **without** replacement (superselection violated, reader-vs-heredity collapsed) — *did not occur (machine-exact null)*; or metric readability never nonzero (reader empty) — *did not occur*. · **→** `reading-transition`, `escape-degenerate-replication`, `battery:strata-current` (sibling sort-vs-mint discriminator), the cross-rule; receipts §Readability battery; `pa:transverse-decomposition`, `pa:cycle-affinity`, `pa:von-neumann-automata`. *Built 2026-06-21 on the `strata_sort` / `composition_law` machinery; all kill-switches pass synthetically — the real-substrate reader is the owed instance.*  

* `battery:circulation-capacity` **\[battery\]** -- *verdict:* the kill-gated $K(C)$ test, **built and run on KaiABC** (`experiments/kaiabc_capacity.py`; the reduced Rust 2007 phosphoform ring U->T->ST->S->U with the measured Tables S1/S2). Both make-or-breaks cleared. **Kill #1 (K is philosophy):** the rate factorization $k_{XY}(S)=k^0+k^A A/(K_{1/2}+A)$, $A=\max(0,[\text{KaiA}]-2S)$ is the model's own, so the constants (archive) separate choice-independently from the mean-field $A$(collective $S$) modulation (circulation); survives KaiC cooperativity (mean-field, scramble-invariant). **Kill #2 (K=b1?) -- VINDICATED:** measured constants -> sustained limit cycle (period $\approx20.7$ h); forward cyclic current $J_{\text{cyc}}=+0.025\,\mu$M/h equal on all four edges (Kirchhoff) that **collapses to 0** at $[\text{KaiA}]=0$; winding **structure-locked** (0 sign-flips / 26 oscillating rate-deformations); $b_1=1$; Floquet multipliers $[1.000,0.046,6.6\times10^{-5}]$ -> exactly one marginal (the phase) $\Rightarrow K_{\text{metric}}=1$, $K=b_1+1=2>1$. **The discriminator:** the frozen-operator affinity is negative at high KaiA and flips sign with $S$ ($\operatorname{sign}\mathcal{A}=\operatorname{sign}$ frozen NESS current -- operator calibration), while the limit cycle runs forward -- the protected current is **feedback-generated, not a frozen-rate bias** (cf. `battery:chiral-rotor-triad`). · **↑** the same kill-gated readout returning $K>b_1$ forced-not-fitted on a **real second** oscillator -> promotes `circulation-held-capacity` toward the curve. · **✗** a maintained record-free circulation where $K=b_1$ exactly (no turnover-stable slow coordinate beyond the topological count), or where the archive/circulation cut is choice-dependent -- $K$ adds nothing. · **→** `circulation-held-capacity` (the object it anchors), `escape-degenerate-replication`, `self-referential-closure`; receipts §Real-substrate instances (KaiABC); `pa:ness-currents`, `pa:cycle-affinity`. *Built + run 2026-06-24 on the `composition_law` Markov machinery + an ODE/Floquet layer; the SOM was fetched to read Tables S1/S2 directly. **Second substrate landed (PO reaction, 2026-06-24, `bfso_capacity.py`):** the BFSO model -- record-free, oscillation / enzyme-conservation($10^{-14}$) / drive-collapse validated -- gives $K=b_1(3)+\text{phase}(1)=4$, distinct via the two coupled enzyme loops; the 2-torus ($K_{\text{metric}}=2\Rightarrow K=5$) is literature-documented but not reproduced without rate-tuning, so not claimed.*
### Staked — planted in the core, un-instanced (mirrors the receipts `staked` tag — I1)

* `staked:gfdr-two-step` **\[staked\]** — *verdict:* near-threshold FDR is two-step; a short-lag $X{=}1$ is not below-threshold. · **↑** a second instance — recurrence of the confusion, or a real glass/quantum/brain substrate exercising the two-step. · **✗** a substrate where short-lag $X{=}1$ genuinely *is* below threshold. · **→** receipts §Two FD frames — two-step.  
* `staked:auto-tuning` **\[staked\]** — *verdict:* a substrate's slow rates auto-tune to the diagonally-stabilising weight $W=\\mathrm{diag}(\\gamma\_{\\text{ref}}/\\gamma\_{s,i})$. · **↑** a substrate requiring diagonal stability shown to auto-tune to the inverse form. · **✗** a stable maintained state on a non-PSD frustration-free topology whose measured rates give an indefinite $W\\gamma+\\gamma^TW$. · **→** receipts §Auto-tuning.  
* `staked:memory-collapse` **\[staked\]** — *verdict:* $\\beta\\approx1-\\varepsilon$ near the marginal point. · **↑** $\\beta(\\varepsilon)$ measured near the marginal point on a real hierarchical substrate, linear with both endpoints respected. · **✗** $\\beta$ departs from $1-\\varepsilon$. · **→** receipts §Memory collapse; `character.md` Coarse-graining. · *shared substrate (2026-06-13):* a **sleeping nervous system** is a candidate for both this and `consolidation-ascent`; the duty cycle's **gap-burstiness** is a third, independent register on the same $\\beta$ (alongside the aging exponent and the kernel), tightening the **$\\beta$-collapse** — the sharpest falsifier — when measured there.

### Recently crossed the line

Promoted this campaign, now in the core (recorded in receipts §Corrections and promoted refinements): the **central commitment** and **two-frame construction** (on rock-paper-scissors); the **deformation chart** and **homochirality** (on rock-paper-scissors and the homochiral triad); the **exact two-frame magnitude identity** (a forced-not-fitted derivation on the rotational-OU testbed); **composite branch** (on the fuel-driven DNA reaction network — the manifold and closure lifts both crossed, now `character.md` §The space of characters and §Composition under coupling); and **branch survival** (the racemic-saddle quasipotential $\\Delta V\\approx0.018$ on the homochiral triad, separated from $I(0)$ by noise-scaling and by controlled embedding — receipts §Branch-survival barrier). The $\\mu$-sweep is run: branch survival is born at $\\mu\_c=(1+a+b)/3=0.833$ by **competitive exclusion**, with $\\Delta V\\propto(\\mu-\\mu\_c)$ linear (the pitchfork $(\\mu-\\mu\_c)^2$ superseded) and zero below threshold. **Both owed items now paid (receipts §Branch-survival barrier):** the multiplicative-noise robustness (demographic $\\sqrt{x}$ noise — $\\Delta V$ rescales to $\\approx0.273$ but existence and protection survive, so not an additive artifact), and a second independent thermalized-crossing substrate — the **co-handed twin-cycle** ($\\mathcal{A}\\approx21.8$, $\\Delta V\\approx0.018$), which instances `both` through a spontaneously broken **exchange** ($S\_2$) symmetry, *not* parity. Decisive independence: sign($\\mathcal{A}$) is preserved across the branch flip (both clusters co-handed) where the homochiral parity flip reverses it — the two survivals shown orthogonal a way the parity instance structurally cannot. The competitive-exclusion mechanism is **handedness-blind** (μ-sweep): $\\mu\_c$ and the linear $\\Delta V(\\mu)$ are machine-precision identical across exchange and parity, since the breaking mode is uniform-within-cluster — so the open pitchfork-vs-competitive-exclusion question resolves the same under both SSB types. `experiments/twin_cycle_corner.py`, `twin_mu_sweep.py`. And **`both` across a second bifurcation mechanism**: an autocatalytic (Kondepudi–Nelson) substrate gives a genuine **soft pitchfork** — $ee\_*^2\\propto(k\_{1c}-k\_1)$ linear ($R^2{=}1$), barrier $\\Delta U\\propto ee\_*^4\\propto(k\_{1c}-k\_1)^2$ **quadratic** (vs the twin's hard/linear); adding an $a\\neq b$ 3-cycle opens a **coexistence window** $ec\\in\[0.05,0.20\]$ that mints $\\mathcal{A}\\approx0.6$–$3.5$ nats while the soft pitchfork holds ($ee\_*^2$-$R^2{=}1$, $\\Delta U\\propto ee\_*^4$ $R^2{=}0.997$, reset \~50/50); past $ec\\gtrsim0.25$ the cycle destabilises the branch (it *participates* in the bifurcation, not merely decorates it). Supported claim: **symmetry breaking does not fix the barrier scaling — the saturation mechanism does** (two distinct constructions, not yet "mechanism-independent"; receipts §Branch-survival barrier, §Two-survivals plane). `experiments/autocat_pitchfork.py`, `autocat_both.py`. **Cross-stratum minting (2026-06-18):** the union-cycle holonomy form (`𝒜_union = ∮Γ_AB·dx`, exactly the cross term when the parts are non-circulating), the skew-coupling fact (no metric work, but necessary-not-sufficient — a Hamiltonian rotation is skew yet `𝒜 = 0`), and stratum-agnostic minting promoted to core as forced derivations (§The minting claim, §The space of characters, §Motion and proximity, §Control parameter; receipts §Composition). The smallness regime, the depth law, and the substrate stay below the line in `selective-coupling-class`. Also landed (clarity / forced consequence): the **two irreducible sectors** compression (abstract, §Object, falsifier added — a character is two non-interconvertible kinds of information, all other observables their readings) and the **cascade closure consequence** (no new *kind* of information up the tower; §Cascade; receipts §Two irreducible sectors).

---

## Tombstones — discarded false starts (kept so they are not re-explored)

* `rejection-anchor-as-new-invariant` — the "rejection" field's barycenter / boundary first-moment / a **flow-resident number** read as a *new* coordinate-independent invariant (two outbound rounds, 2026-06-28). **Died:** round 1 — the barycenter is a *derived coordinate statistic* (the measure/current is fundamental), and the intrinsic form $\\int_{\\partial M}n\\,d\\sigma\\equiv0$ by the divergence theorem (a fake-NaN). Round 2 — the interior version is **not** a current (contracting directions don't close into homological cycles) and **not** generic (a genuine fast anchor needs a spectral gap / normal hyperbolicity; without it = the bare ergodic average). **Replaced by:** the existing machinery — the rejection/contraction frame is the **stable foliation / Oseledets bundle $E^s$** (`pa:oseledets-pesin`, `pa:clv`), the metric sector's intrinsic geometry dual to the protected current, barred from the escape barrier (`pa:transverse-decomposition`, already citing FW + Maier–Stein); its existence-condition (the gap) **is the marginal point** (`wall-as-type-boundary`, `B-marginal`) — the anchor dissolves at $\\varepsilon\\to1$. No new object; receipts §Rejection-anchor.  
* `flip-cost-across-zero` — protection-as-a-flip-cost across the sign-erased zero. **Died:** formulating protection as a measurement that visits the affinity-zero is malformed by the open-interval discipline (the zero is never attained). **Replaced by:** the interior sign-stability test (`battery:sign-interior`). The $\\ge1$-bit flip floor fired entirely — a sign flip is a reversible bijection (no erasure, no Landauer floor).  
* `sign-kill-bisection` — the can-kill-can't-vindicate standdown that split the test into kill-only. **Died:** a boundary-gated formulation; the framework is degenerate at the boundary, rich in the interior. **Replaced by:** the interior, can-kill-and-vindicate `battery:sign-interior`.  
* `thm9-coefficient-series` — the Boolean-deviation bound→equality as an analytic $1/D$ coefficient series. **Died:** two passes converged that the seam is a crossover scaling function, not a series; the series stayed fitted-not-forced. **Replaced by:** the scaling-collapse target $(\\alpha, F)$.  
* `alignment-dependent-active-stress` — the alignment-dependent form for the active-stress fingerprint. **Died:** it captures only the flocked-state correction, not the leading scaling. **Replaced by:** the alignment-independent fingerprint $\\beta/\\alpha\\sim v\_0^2\\tau\_R/D\_{\\text{trans}}$.  
* `quadratic-lyapunov-candidate` — a quadratic Lyapunov form. **Died:** fails the critical-point test ($\\partial V/\\partial x\_i|\_{x^\*}\\ne0$). **Replaced by:** the relative-entropy form (receipts §Frustration-free Lyapunov).  
* `current-aids-escape` — the protected current read as a *resource that lowers the branch-escape barrier* ($\\Delta V$ falling with the affinity $\\mathcal{A}$). **Died:** the exact instanton (gMAM, validated on Maier–Stein to $0.01%$) gives a $\\sigma\\to0$ quasipotential **flat** in $\\mathcal{A}$ ($\\Delta\\hat S\_H \=-0.4%$ over $\\mathcal{A}:0\\to21.8$ vs the finite-$\\sigma$ slope's $-17%$), the instanton never bending off the symmetric subspace — because the current is **exactly transverse** to the escape mode at the saddle ($|\\cos(J,e\_u)|\\sim10^{-15}$, symmetry-protected: escape and current in different irreps, broken $\\propto\\delta$ by a $Z\_3$ break). The current's own ✗ ("gMAM shows the finite-window slope mis-ordered the true actions") fired. **Replaced by:** an imported reading — the **transverse-decomposition theorem** leaves $\\Delta V$ invariant; the measured drop is the **irreversible Eyring–Kramers prefactor** (receipts §Branch-survival barrier; `pa:transverse-decomposition`). The two-survivals separation is *reinforced* ($\\Delta V\\perp\\mathcal{A}$ by a symmetry theorem); a current reaches a barrier only where coupling mixes the irreps (the in-substrate Maier–Stein turn-on, `gmam_mixing_test.py`).
* `curvature-as-coupling-bias` — the surviving symmetry-allowed $O(\\mathcal{A}^2)$ response coefficient read as a *geometric curvature* (quasipotential-Hessian / thermodynamic-Ruppeiner / Berry-pumping / spectral-sheet / unfolding), promotable only if a named result *forced* it universally. **Died (2026-06-21 — a clean negative):** three independent outbound derivations converge that the coefficient is the **leading analytic response coefficient, not a universal geometric curvature** — it is *observable-dependent* (two observables on one process give different coefficients; a curvature scalar would not), the FW quasipotential is $\\mathcal{A}$-invariant (the transverse theorem) so every curvature built from it is $\\mathcal{A}$-invariant while the coefficient is nonzero (hence it is **not** the quasipotential's curvature — the strongest single obstruction), and each candidate fails structurally (Berry needs a cyclic loop in two parameters — here one static $\\mathcal{A}$, vanishing at a point; no exceptional point at $\\mathcal{A}\=0$; $\\mathcal{A}\=0$ a regular point, not a bifurcation; the thermodynamic object is a *metric component*, not its curvature). Closed form (forced, not fitted): a Rayleigh–Schrödinger resolvent sum \= an Eyring–Kramers prefactor trace \= the driven ring's equilibrium covariance (`character_receipts.md` §"Local geometry's coupling role, bounded"). The logged parameter-space-vs-state-space caveat **held** (all three kept them distinct; the coefficient lives cleanly in neither as a curvature). **Replaced by:** the already-safe core form (§Motion and proximity, "Local geometry, bounded") — local geometry *modulates* coupling strength through a second-order response coefficient, with **no** geometric-curvature import; the negative *reinforces* the transverse-decomposition theorem (the $\\mathcal{A}$-invariance of $\\Delta V$ is exactly what bars the strongest geometric candidate). Returns: `docs/research_prompt_curvature_as_coupling_bias.md`.  

