# Character — imported results (the provenance ledger)

Claim boundary, stated by exclusion. Every object below is **imported, not the
framework's** — the framework adds no object, only the *reading* (the falsifiable
cross-substrate transfer onto the affinity `a`) and the *bindings* and *falsifiers*. What is
**not** in this ledger is the residual the framework owns, set out in
[`character.md`](character.md) §"What the translation leaves standing": the forcing of the
imports onto the single parameter `a` and the over-determination that makes the alignment a
test; the umbrella term *character*; the identification of the deformation algebra as the
tangent space of a single morphospace, and the proximity laws; the binding
(graph-frustration ⟺ spectral complex pair ⟺ cycle affinity) read in its falsifiable
direction; identity as the protected coordinates sustained as a circulation; and the closure
`⊗` with its minting claim and one real instance.

Each row: `key` — **imported result** · *imports* (what is borrowed) · *handle* (where it
lands in the framework, in standard terms) · *adds* (the reading, owned). The `key` is the
bare handle the receipts ([`character_receipts.md`](character_receipts.md)) resolve against.
Granularity is coarse — named/eponymous results only; generic mathematical vocabulary is
untracked (see TOOLKIT).

---

## Algebra & contraction
* `anf-ring` — **Reed–Muller / ANF over GF(2)** · imports: every Boolean function has a unique $\{\oplus,\land,1\}$ form, $\neg x=x\oplus1$ · handle: the Boolean limit (infinite drive) · adds: finite drive is the deformation of this ring, with the affinity $a=\ln(G_0/L)$ as the deformation coordinate — *not* the unit $1$ (the $1\leftrightarrow G_0$ identification is a type error).
* `banach-fixed-point` — **Banach fixed-point theorem** · imports: unique fixed point under contraction · handle: the coarse-graining contraction modulus $\varepsilon=\|\mathcal{C}\|_{op}<1$ · adds: $\varepsilon$ is *derived* as the infrared linear-stability eigenvalue of the level-to-level map (not posited); the marginal point is $\varepsilon\to1$.
* `krein-rutman` — **Krein–Rutman theorem** · imports: isolated dominant eigenmode of a positive contracting operator · handle: the metric on coarse-grained characters $\rho([A],[B])$ · adds: distance between coarse-grained classes = the dominant-eigenmode amplitude surviving $\varepsilon^{-n}$ rescaling.
* `power-iteration` — **power iteration (von Mises)** · imports: rescaled-limit convergence to the dominant mode · handle: the $\lim_n\varepsilon^{-n}\|\mathcal{C}^n\cdot\|$ construction · adds: same.
* `nonhermitian-ep` — **non-Hermitian degeneracy / exceptional points (Kato; Heiss; Berry)** · imports: spectral coalescence near gap closure, $\omega^2=\omega_0^2-(\delta/2)^2$ · handle: the shear/detuning direction of the deformation algebra; the metric-proximity coalescence rule · adds: the exceptional point is the spectral *onset signature* of the circulating mode, **not** the source of its protection.
* `splay-state` — **$Z_2$ chiral splay in repulsively-coupled oscillator rings (Sakaguchi–Kuramoto frustrated splay; BZ micro-droplets, Fraden/Tompkins 2014; STNO / nanolaser / $Z_n$-clock arrays)** · imports: spontaneous CW/CCW choice of firing order (discrete $Z_2$ chirality), an internal phase-space cycle, chiral relaxation gapped at $\mathrm{Re}\sim-K$ (no Goldstone, far from the exceptional point) · handle: a candidate self-lighting, gapped single-unit substrate · adds: a clean single-unit instance whose discrete chirality is a *real* gapped spectrum (no antisymmetric seed), so it does **not** platform a composite cycle under even-parity coupling.
* `goldstone-mode` — **Goldstone's theorem / soft-mode gapping (Goldstone–Nambu; Watanabe–Murayama, dissipative case)** · imports: breaking a *continuous* symmetry gives a gapless (soft) phase mode; a *discrete* symmetry, non-reciprocity, or a mass term gaps it · handle: the damping of an internal chiral phase mode — soft (fragile) vs gapped (robust) · adds: a composite-forming substrate needs the chiral mode *gapped* (discrete-chirality / non-reciprocal / far-from-EP), not a soft near-Goldstone mode.

## Signed graphs & frustration
* `signed-balance` — **structural balance of signed graphs (Harary)** · imports: a signed cycle is balanced iff it has an even number of negative edges; a node gauge $\varepsilon\in\{\pm1\}^N$ removes a balanced sign · handle: the topological/circulation sector; the union-graph frustration rule · adds: imbalance is the deformation's sign face; the gauge-irremovable chiral sign is the topological bit, and the union of balanced graphs can be unbalanced (proximity mints a protected current).
* `frustration` — **frustration in spin glasses (Toulouse)** · imports: the concept of an unsatisfiable signed cycle · handle: the frustrated cycle behind the protected current · adds: the sustained-NESS reading — frustration forces a *circulating current*, not a static ground-state degeneracy.
* `ksat-threshold` — **sharp threshold for sign-frustrated cycles in random $k$-SAT (Mézard–Parisi–Zecchina)** · imports: sharp SAT/UNSAT threshold under constraint pressure · handle: the capacity sharp threshold; the unsatisfiable core · adds: marks structure unsustainable at any drive.
* `hopfield` — **associative-memory capacity ceiling (Hopfield; Amit–Gutfreund–Sompolinsky)** · imports: $\sim N$ pattern capacity, sharp memory cliff · handle: the $\sqrt{D}$ capacity ceiling · adds: modular sparsification read as the $\sqrt{D}$ shadow.

## RG & projection
* `functional-rg` — **Wilson–Polchinski / Wilson–Kadanoff functional RG** · imports: running-coupling flow on a space of generators with an infrared fixed point · handle: the coarse-graining flow (type-identity) · adds: the Boolean limit as the infrared fixed point; capacity transported under coarse-graining; fractional-RG extension when the memory exponent $\beta<1$.
* `mz-projection` — **Mori–Zwanzig projection** · imports: projection onto slow variables, memory kernel for the rest · handle: the slow-manifold projection $\Pi_{\text{slow}}$ · adds: the contraction $\mathcal{C}$ is the heat-tax flow on the $\Pi_{\text{slow}}$-projected generators.
* `slaving` — **Haken slaving principle (Synergetics)** · imports: fast modes enslaved to slow order parameters · handle: tower priority ordering; center-manifold reduction · adds: tower-level slaving sets the priority ranking.
* `cavity-frozen-core` — **cavity-method frozen core (Krzakala et al.)** · imports: frozen vs free variables in the cavity solution · handle: the Boolean-limit frozen core vs the near-threshold free region · adds: identifies the coarse-grained classes with cavity regions.

## Laser / driven-open
* `threshold-trichotomy` — **laser / driven-open-system threshold (Haken; Sieberer–Buchhold–Diehl)** · imports: below/near/above-threshold trichotomy of a driven open mode · handle: the threshold regimes ($a<0$ / $a\approx0^+$ / $a\gg0$) · adds: the trichotomy read as maintenance of a steady state under load.
* `gain-depletion` — **multi-mode laser gain depletion (Lamb; Haken)** · imports: $G_0^{\text{eff}}=G_0/(1+\sum\rho_j/\rho_{\text{sat}})$, cubic cross-saturation · handle: the metric-proximity *competition* rule · adds: the discrete composite catalogue as the fixed points of this kernel.
* `relaxation-oscillation` — **class-B laser relaxation oscillation (Haken; Siegman)** · imports: complex-pair linearisation, damping/frequency/$Q$ · handle: the relaxation-oscillation observables $\gamma_{RO},\omega_{RO},Q$ · adds: $Q$ as a quantity that *flows* with the operating point (the measurement-discipline audit unfroze it: $\gamma_{RO}=(\gamma_s/2)e^{a}$).
* `schawlow-townes` — **Schawlow–Townes linewidth** · imports: spontaneous-emission line broadening near threshold · handle: the near-threshold width floor · adds: drive-axis substrates carry this as the irreducible near-threshold floor.

## Nonlinear dynamics & bifurcation
* `lotka-volterra` — **Lotka–Volterra / two-mode laser equations** · imports: competitive/cooperative population dynamics, carrying-capacity coupling · handle: the universal two-mode kernel · adds: substrate-neutral reading of the kernel as the maintenance dynamics.
* `may-leonard` — **cyclic three-species Lotka–Volterra (May–Leonard)** · imports: heteroclinic cycle, no interior equilibrium · handle: the minimal frustrated 3-cycle · adds: a forced imbalanced triad is the minimal carrier of a gauge-irremovable circulation (the rock-paper-scissors real instance).
* `cyclic-attractors` — **cyclic attractors (Hofbauer–Sigmund)** · imports: comprehensive cyclic-attractor dynamics · handle: the circulating-focus / limit-cycle forms of the protected current · adds: same sustained-NESS reading.
* `bifurcation-normal-forms` — **transcritical / pitchfork / Hopf normal forms (Strogatz; Guckenheimer–Holmes; Kuznetsov)** · imports: codim-1 normal forms · handle: the regime boundaries (threshold-on at $a=0$, etc.) · adds: the framework's regime boundaries are these bifurcations.
* `codim2-normal-forms` — **codim-2 normal forms (Bogdanov–Takens; Bautin; Arnold; Kuznetsov)** · imports: the codim-2 unfoldings (cusp, generalized-Hopf, fold-Hopf, B–T) · handle: the pitchfork–Hopf intersection · adds: a Bogdanov–Takens point is forced where the framework's curves cross.
* `center-manifold` — **center-manifold theory** · imports: slow-manifold reduction at marginal stability · handle: the near-threshold tangent space; the aging segment · adds: near-threshold aging is algebraic settling on the center manifold ($P_s$ amplitude, $\alpha_s$ residual scaling).
* `nrt-chaos` — **chaos via quasi-periodicity (Ruelle–Takens; Newhouse–Ruelle–Takens)** · imports: a strange attractor exists $C^2$-near a 3-torus (topological genericity, existence, not measure-forcing) · handle: the quasiperiodic *route* past the marginal point · adds: names a route, not a forcing — post-marginal chaos is delay-driven, and the 3-torus is robust under weak coupling.
* `delay-hopf` — **delay-induced bifurcation (Mackey–Glass)** · imports: Hopf in a delay differential equation · handle: the per-ascent delay equation · adds: as the marginal point is approached the delay diverges and auto-satisfies the Hopf condition.
* `fenichel-nhim` — **normally hyperbolic invariant manifolds (Fenichel; Hirsch–Pugh–Shub)** · imports: persistence of a reduced manifold while normal contraction dominates the tangential rates (the spectral-gap condition) · handle: each cascade plateau as a persistent invariant manifold; $\varepsilon<1$ = normal hyperbolicity · adds: the marginal point ($\varepsilon\to1$) is loss of normal hyperbolicity (the reduction fails) — distinct from, and not sufficient for, forced chaos.
* `clv` — **covariant Lyapunov vectors (Ginelli et al. 2007)** · imports: norm-independent stable/tangent/unstable bundles + the minimum angle $\theta_{\min}$ between them · handle: the plateau-vs-boundary diagnostic ($\theta_{\min}\to0$ at loss of hyperbolicity, bounded on a normally-hyperbolic torus — no false positive) · adds: the pre/post observable for the marginal point as a type boundary.
* `dissipative-kam` — **dissipative KAM / robust quasiperiodic tori (Broer–Huitema–Takens; Grebogi–Ott–Yorke 1985)** · imports: Diophantine tori persist under weak dissipative perturbation; the 3-torus is the common attractor, chaos uncommon · handle: weak-coupling cascade rhythms hold a torus · adds: corrects "$N\ge3\Rightarrow$ forced chaos" to fragile-not-generic.
* `nonreciprocal-transition` — **non-reciprocal phase transitions (Fruchart–Vitelli)** · imports: exceptional points, chiral travelling states under $\gamma_{AB}\ne\gamma_{BA}$ · handle: the non-reciprocal kernel; the minted (vs pumped) current · adds: the limit-cycle / travelling-wave structure of non-reciprocal coupling.

## Synchronization
* `kuramoto` — **Kuramoto phase model / phase reduction** · imports: phase coupling, $K_c$ onset, $1/(2Q)$ suppression · handle: the metric-proximity *locking* rule, $K_{AB}\propto(1+4Q^2)^{-1/2}$ · adds: two independent NESS transitions (amplitude onset vs phase onset) and the $Q$-crossover.
* `adler-locking` — **injection locking / Arnold tongue (Adler 1946; Pikovsky–Rosenblum–Kurths)** · imports: a coupled oscillator stays phase-locked while detuning stays below a range *linear in coupling* ($|\Delta\omega|<\omega_L\propto K$) · handle: the robustness of a composite cycle to detuning (proximity locking) · adds: normal-misalignment read as detuning, so a protected cycle survives iff it stays inside the locking range, with the cycle birth/death an exceptional point.
* `master-stability` — **master stability function (Pecora–Carroll)** · imports: sync stability from the Laplacian spectrum · handle: homogeneous-mode synchronization · adds: the gauged-$\gamma$ Laplacian as the relevant operator.
* `generalized-msf` — **generalized / cluster-sync MSF (Sun–Bollt–Nishikawa; Sorrentino)** · imports: heterogeneous-network sync stability · handle: heterogeneous multi-mode kernels · adds: per-mode heterogeneity (application owed).
* `chimera` — **chimera states (Kuramoto–Battogtokh; Abrams–Strogatz)** · imports: coexisting coherent/incoherent sub-populations · handle: coexisting above-/below-threshold sub-populations · adds: chimera as a substrate-instancing falsifier.

## Stochastic thermodynamics
* `crooks-ft` — **rate-ratio fluctuation theorem (Crooks); affinity (Onsager; De Donder)** · imports: $\ln(\text{fwd}/\text{bwd})$ = per-transition entropy production; $P(\sigma)/P(-\sigma)=e^\sigma$ · handle: the affinity $a=\ln(G_0/L)$ · adds: the log form of $a$ is forced; threshold-symmetry, additivity, and the $\ln 2$ quantum are one structure.
* `cycle-affinity` — **network-thermodynamics cycle affinity (Schnakenberg)** · imports: $\mathcal{A}=\oint v/D=\ln(\prod_+k/\prod_-k)$, gauge-invariant · handle: the cycle affinity $\mathcal{A}$, the intrinsic (self-probe) ruler · adds: drive-independence lives in $\mathcal{A}$ while the current magnitude $J_{ss}$ flows with $a$.
* `harada-sasa` — **Harada–Sasa equality** · imports: integrated FDR-violation $=\langle\sigma\rangle$ · handle: the bridge between the two fluctuation–dissipation frames · adds: $\int(\text{FDR departure})=\langle\sigma\rangle=J\cdot\mathcal{A}$ across both frames (closed to machine precision on the rotational-OU testbed).
* `tur` — **thermodynamic uncertainty relation (Barato–Seifert; Horowitz–Gingrich)** · imports: $\mathrm{Var}(J)/\langle J\rangle^2\ge2k_B/\langle\sigma\rangle$ · handle: the intrinsic-frame tightness $\mathcal{T}$ · adds: $\mathcal{T}$ as a substrate-class fingerprint (a falsifier).
* `sagawa-ueda` — **extended second law (Sagawa–Ueda)** · imports: $\langle\sigma\rangle\ge-\Delta I$ · handle: the bit/affinity coupling · adds: readout-maintaining states sustain at lower $a$ by paying mutual information.
* `gc-ft` — **steady-state fluctuation theorem (Gallavotti–Cohen; Lebowitz–Spohn)** · imports: large-deviation symmetry of entropy production · handle: the orbit affinity $\mathcal{A}=\ln(\mathcal{P}_+/\mathcal{P}_-)$ · adds: the limit-cycle affinity reading.
* `ness-currents` — **NESS probability currents (Zia–Schmittmann)** · imports: stationary currents without detailed balance · handle: the protected current as a forced NESS · adds: the stationary state is irreducibly circulating.
* `benjamin-feir` — **Benjamin–Feir–Newell instability of the complex Ginzburg–Landau / coupled Stuart–Landau chain (Benjamin–Feir 1967; Newell; Aranson–Kramer 2002)** · imports: the synchronized plane-wave state with shear $c$ + reactive coupling $b$ destabilizes for $1+bc<0$ · handle: a *non-delay* loss-of-normal-hyperbolicity route ($\theta_{\min}\to0$, $\lambda_{\max}>0$) · adds: the shear-driven closure-loss boundary; instances *loss-of-hyperbolicity $\ne$ forced chaos* (the $N=3$ cascade reaches the boundary without chaosing).
* `esposito-coarse-graining` — **coarse-graining of stochastic thermodynamics (Esposito 2012; Polettini–Esposito effective affinities 2019; Rao–Esposito 2021; Altaner–Vollmer 2012)** · imports: under timescale separation, decimating fast states preserves the slow cycle's effective *affinity* while renormalizing the rates/currents — provided the map respects the cycle homology · handle: the scale axis of the topological bit · adds: the orientation/affinity does not migrate, the current renormalizes; the homology-respecting condition *is* the rewiring boundary.
* `timescale-ep` — **fast/slow entropy-production decomposition (Bo–Celani 2014; Esposito 2012)** · imports: under timescale separation the total entropy production splits $\langle\sigma\rangle_{\text{tot}}=\langle\sigma\rangle_{\text{slow}}+\langle\sigma\rangle_{\text{fast}}$, the hidden part carried by the integrated-out fast variables · handle: the conjugate cascade's down-ledger (the hidden EP of the level integrated out going up) · adds: the hidden EP is the protected (transverse) circulation's running cost, aligned with the metric/protected split by Schur — order built up is paid for by erasure exported down.
* `sde-lyapunov` — **stochastic differential equations / continuous Lyapunov (Gardiner; Øksendal; Thygesen)** · imports: the diffusion tensor and the NESS covariance $P$ from $\dot P=MP+PM^\top+Q$ · handle: the noise extension of the deformation algebra · adds: the noise axis closes on the Lyapunov solution.
* `ness-fdr` — **NESS FDR with extra response term (Speck–Seifert)** · imports: additive velocity-response term in driven FDR · handle: the two conjugate FDR frames · adds: the external-probe vs self-probe verdict-agreement claim.
* `kolmogorov-reversibility` — **Markov reversibility criterion (Kolmogorov)** · imports: detailed balance $\Leftrightarrow$ every cycle's fwd/bwd rate-product equal · handle: the detailed-balance discriminator · adds: a sustained cycle current $\Rightarrow$ nonzero affinity.
* `reversible-spectrum` — **detailed-balance generators have real spectrum (classical Markov spectral theory; Andrieux 2011; Potoyan–Wolynes 2014)** · imports: a reversible generator is self-adjoint under the stationary-measure inner product $\Rightarrow$ real spectrum; contrapositive, a complex-conjugate pair $\Rightarrow$ broken detailed balance ($\mathcal{A}\ne0$); the converse *fails* (broken DB only *may* give a complex pair) · handle: the spectral register of the binding · adds: the implication is **one-way** — the complex pair is the underdamped *signature* of $\mathcal{A}\ne0$, suppressible to a real overdamped circulation; protection lives in $\mathcal{A}$, not the spectrum.
* `landauer` — **Landauer bound** · imports: irreversible erasure costs $\ge kT\ln2$ · handle: the population (amplitude) bit's erasure floor · adds: nothing on the topological face — a sign flip is a *reversible* bijection (no erasure $\Rightarrow$ no Landauer floor).
* `bennett-reversible` — **reversible computation (Bennett)** · imports: logically reversible operations cost nothing fundamental · handle: the population bit is reversibly flippable · adds: the topological-bit sign flip is *also* reversible (a bijection / braid) $\Rightarrow$ free.
* `dilution-structure` — **$(1-\eta)$ second-law dilution (Esposito–Van den Broeck)** · imports: retained-fraction dilution of dissipation · handle: the per-level heat tax $\alpha(\varepsilon)=\alpha_0(1-\varepsilon)$ · adds: per-level heat $\to0$ as $\varepsilon\to1$ while cumulative mass diverges (the marginal point as a thermodynamic singularity).

## Escape & quasipotential
* `transverse-decomposition` — **transverse (orthogonal) decomposition of the Freidlin–Wentzell quasipotential + the irreversible Eyring–Kramers prefactor (Freidlin–Wentzell 1998 §4.3; Graham–Haken 1971; Bouchet–Reygner 2016; Graham–Tél 1984; Ao 2004; Maier–Stein 1993)** · imports: noise-activated basin escape has rate $\langle\tau\rangle\sim K\,e^{\Delta V/\sigma^2}$ with $\Delta V$ the FW quasipotential (Kramers); for a drift split $b=-a\nabla V+l$ with $l$ transverse to $\nabla V$ ($\langle l,\nabla V\rangle=0$), $V$ solves the *same* Hamilton–Jacobi/eikonal equation as the gradient flow $-a\nabla V$, so $\Delta V$ and the leading exponent are identical to the gradient system's, and the circulatory $l$ enters only the sub-exponential prefactor $K$ (Bouchet–Reygner: the saddle's full-Jacobian unstable eigenvalue + a non-Gibbsian path integral $\int(\mathrm{div}\,l+\langle A,\nabla V\rangle)$); transversality enforced by a symmetry — escape mode and current in different irreps — makes the cancellation exact by Schur (Graham–Tél); the complementary case $\langle l,\nabla V\rangle\ne0$ along the path lets the instanton bow out and *lower* $\Delta V$ (Maier–Stein) · handle: the metric $\perp$ topological split read at the **large-deviation** level — the protected current $\mathcal{A}$ vs the branch-survival barrier $\Delta V$ (§The two-survivals plane); the classical large-deviation face of the decoherence-free-subsystem identification (§The two tangent sectors) · adds: a protected current is **barred from the metric sector's escape barriers and confined to their prefactors** — it sets the *rate* of a branch crossing, never the *cost*; so the two survivals are orthogonal by a symmetry theorem (Schur), not merely in existence, and an apparent "current lowers the barrier" is a prefactor effect; a current reaches a barrier only where coupling/proximity mixes the irreps (§The cross-rule) — the Maier–Stein boundary. **Instanced in QEC** (receipts §QEC transverse decomposition): in the surface code the CSS X/Z split *is* the exact transverse separation — a phase current leaves the bit-flip logical-barrier exponent invariant to all orders while firing stabilizers, and Y-mixing leaks it $\propto\delta$; a *same-sector* current has no protecting symmetry and always leaks (every detectable error lies on a minimum-weight logical), confirming the orthogonality is **symmetry-protected, not generic**.

## Glassy / aging / estimators
* `ck-aging` — **aging in spin glasses (Cugliandolo–Kurchan; Crisanti–Ritort)** · imports: two-step aging FDR, effective temperature, the external-frame ratio $X$ · handle: the aging observables $\alpha_s$, $P_s$, $X$ · adds: the threshold-regime trajectory; the load-bearing cross-substrate observables.
* `fdr-estimator` — **field-free FDR estimators (Ricci-Tersenghi; Chatelain)** · imports: self-overlap + staggered-field susceptibility (not collective magnetisation) · handle: the FDR measurement recipe · adds: a stable $X$ near criticality requires this estimator.
* `rfim-avalanche` — **mean-field RFIM avalanches (Dahmen–Sethna; Sethna Barkhausen)** · imports: at-$H_c$ avalanche exponent $\to3/2$ · handle: avalanche apparatus validation · adds: a positive control for the $\tau\approx3/2$ falsifier.

## SOC / branching
* `soc` — **self-organized criticality (Bak–Tang–Wiesenfeld)** · imports: self-tuning to a critical attractor · handle: the near-threshold attractor ($a\approx0$) · adds: the near-threshold state is the generic attractor of feedback-coupled NESS.
* `branching-process` — **Galton–Watson branching (Harris)** · imports: critical branching ratio, extinction/survival · handle: $\mu=e^{a}$; the marginal-point branching · adds: $\mu\to1$ at $a=0$ (drive axis) and tower-branching $\to1$ at $\varepsilon=1$ (scale axis).
* `directed-percolation` — **mean-field directed-percolation $\tau=3/2$ (Stauffer–Aharony)** · imports: mean-field avalanche exponent · handle: $\tau\approx3/2$ at $a=0$ · adds: the substrate-graph dimension fixes the empirical exponent.

## Information theory / geometry / computational mechanics
* `channel-capacity` — **channel capacity (Shannon–Hartley)** · imports: $C\sim B\log_2(1+S/N)$ · handle: active-probe capacity $\sim\gamma_{RO}\log_2(1+Q)$ · adds: the informational dual of the precision bound.
* `rate-distortion` — **rate-distortion theorem (Shannon)** · imports: lossy-compression rate; geometric overhead series · handle: $\varepsilon$ as compression rate; $\Phi_{\text{total}}=\Phi^{(0)}/(1-\varepsilon)$ · adds: the compression tower's informational ledger.
* `predictive-info` — **predictive information (Bialek–Nemenman–Tishby)** · imports: $I_{\text{pred}}=I(\text{past};\text{future})$ · handle: a third steady-state observable · adds: its scaling as a substrate-class fingerprint.
* `info-bottleneck` — **Information Bottleneck (Tishby–Pereira–Bialek)** · imports: minimal sufficient statistic for prediction · handle: internal-model richness · adds: richness as a *derived* $I_{\text{pred}}$/budget combination (structural reading open).
* `info-geometry` — **information geometry (Amari–Nagaoka)** · imports: Fisher-information statistical manifold · handle: the maintained-state parameter manifold · adds: merge = "the geodesic stays on the above-threshold manifold."
* `hopf-rinow` — **Hopf–Rinow theorem** · imports: geodesic completeness fails at boundaries/obstructions · handle: the $a=0$ metric boundary; the frustrated-cycle excision · adds: the frustrated cycle is a homotopy (topological) obstruction, distinct from the $a=0$ metric singularity.
* `epsilon-machine` — **computational mechanics / $\varepsilon$-machines (Crutchfield; Shalizi–Crutchfield)** · imports: $C_\mu$ structural complexity, cryptic order · handle: $\chi=C_\mu-I_{\text{pred}}$ · adds: cryptic order = encoding overhead = dissipation excess at the optimal limit.
* `still-bound` — **thermodynamics of prediction (Still–Sivak–Bell–Crooks)** · imports: $\langle\sigma\rangle-\langle\sigma\rangle_{\min}\ge\gamma_s\chi$, equality at optimal encoding · handle: the optimal-encoding coincidence · adds: $\chi$ as one quantity in three registers.

## Queueing
* `kingman` — **heavy-traffic limit (Kingman; Iglehart–Whitt)** · imports: $\langle Q\rangle\sim1/(1-\rho)$ · handle: heavy traffic = near-threshold aging · adds: the queue-tail exponent ↔ the FDR aging exponent, two readings of one $\beta$ (coincident at $\beta=1$).
* `fbm-queueing` — **fractional-Brownian / LRD queueing (Norros; Leland et al.)** · imports: $1/(1-\rho)^{\beta}$ generalisation · handle: the anomalous heavy-traffic exponent · adds: heavy-traffic exponent $=g(\beta)=\beta/(2-\beta)$, one register of the data-collapse claim — coincident with $\alpha_s=\beta$ only at $\beta=1$.
* `cobham-priority` — **priority-queue wait (Cobham; Kleinrock conservation)** · imports: $W=W_0/[(1-\sigma_n)(1-\sigma_{n+1})]$ · handle: the tower load-propagation wait · adds: the three load-propagation channels are three faces of one queueing mechanism.
* `littles-law` — **Little's law** · imports: $\langle Q\rangle=\lambda\langle W\rangle$ · handle: the amplitude × rate × timescale relation · adds: holds per priority class at a fixed operating point.
* `erlang-b` — **Erlang loss formula** · imports: $B(c,\rho)$ blocking, M/M/c/c · handle: the soft-vs-hard capacity wall · adds: soft (Erlang) vs hard ($\mathbb{1}[n\ge c]$, $n$ the number of maintained modes) blocking as a substrate-class fingerprint.
* `product-form` — **product-form networks (Jackson; Burke; Kelly)** · imports: $P(\mathbf Q)=\prod_k P_k(Q_k)$ reversible-network factorisation · handle: spatial patch independence · adds: the critical-length breakdown of product form.

## Active matter
* `toner-tu` — **flocking hydrodynamics (Toner–Tu; Vicsek; Simha–Ramaswamy)** · imports: active-stress correction in the ordered state · handle: $f(r)=Cr^2$ · adds: the sign of $C$ (contractile/extensile) sets the heat-tax channel balance.
* `active-hydro` — **soft-active-matter hydrodynamics (Marchetti et al.)** · imports: active-stress framework · handle: maintained units as active-matter units · adds: the active-stress fingerprint as an adoption observable.
* `mips` — **motility-induced phase separation (Cates–Tailleur; Fily–Marchetti)** · imports: clustering of repulsive self-propelled particles · handle: clustering at $\gamma_{AB}\ge0$ · adds: high-Péclet clustering at $\gamma_{AB}\ge0$ as a falsifier (a mechanism absent from the two-mode kernel).
* `swim-pressure` — **swim pressure (Brady; Takatori–Brady)** · imports: $P_{\text{active}}\propto nv_0^2\tau_R/d$, alignment-independent · handle: $\beta/\alpha\sim v_0^2\tau_R/D_{\text{trans}}$ · adds: a fingerprint that survives $r\to0$.
* `green-kubo` — **Green–Kubo relation** · imports: $\tau_R=\int_0^\infty\langle\mathbf u(t)\cdot\mathbf u(0)\rangle\,dt$ · handle: a substrate-neutral correlation time · adds: correlation-time identification across substrate classes.

## Control / adaptive
* `internal-model` — **Internal Model Principle (Francis–Wonham; Sontag)** · imports: a regulator must embed a model of its disturbance · handle: the maintained mode as a plant+controller loop · adds: a four-axis control observable; a frustrated cycle admits no gradient Lyapunov function.
* `volterra-lyapunov` — **Volterra–Lyapunov diagonal stability (Redheffer; Takeuchi; Goh)** · imports: relative-entropy Lyapunov $V=\sum_i[x_i-x_i^*-x_i^*\ln(x_i/x_i^*)]$; diagonal-stability condition · handle: the stability closures · adds: frustration-free = existence of a balancing gauge; PSD is strictly stronger; no gauge $\Rightarrow$ no Lyapunov of this form.
* `consumer-resource` — **consumer-resource model (MacArthur)** · imports: shared-resource competition dynamics · handle: the dynamic-bath inversion · adds: the bath as a dynamical resource projected out.
* `adaptive-dynamics` — **adaptive dynamics (Geritz–Kisdi–Meszéna–Metz)** · imports: trait evolution toward stability · handle: the auto-tuning of $\gamma_{s,i}$ · adds: the substrate auto-tunes to the diagonally-stabilising weight.
* `rescorla-wagner` — **error-driven associative learning (Rescorla–Wagner; Sutton–Barto)** · imports: $\dot V=\kappa(\lambda-V)$ · handle: a reference driver · adds: reward withdrawal $\lambda=0$ = pure-dissipation below-threshold quench (a behavioral falsifier).

## Pattern formation
* `turing` — **reaction-diffusion instability (Turing; Murray; Gierer–Meinhardt)** · imports: $D_BJ_{AA}+D_AJ_{BB}>2\sqrt{D_AD_B\det J}$ · handle: the spatial Turing pattern, wavelength $k_c$ · adds: three substrate-conditional conditions (non-reciprocity + autocatalysis + differential diffusion); the spatial face of the non-reciprocal structure.
* `dissipative-structures` — **dissipative structures (Prigogine–Nicolis)** · imports: order formation past a nonequilibrium threshold · handle: the threshold crossing as formation · adds: the affinity $a$ as the formation control parameter.
* `synergetics-order-param` — **Synergetics order parameter (Haken)** · imports: order-parameter / slaving description of self-organisation · handle: $a$ as the order parameter · adds: the slow order parameter is the maintained mode.

## Fractional / memory
* `caputo-fractional` — **Caputo fractional calculus / Mittag-Leffler (Podlubny; Mainardi)** · imports: $\Gamma(\tau)=\Gamma_0 E_\beta(-(\tau/\tau_c)^\beta)$, power-law tail · handle: the fractional-memory kernel · adds: $\beta=1$ exponential, $<1$ glassy; the memory axis of the deformation.
* `ctrw` — **anomalous diffusion / CTRW (Metzler–Klafter)** · imports: $\langle x^2\rangle\sim t^\beta$, fractional Fokker–Planck · handle: the critical length $\ell_c(\beta)$ · adds: the spatial marginal-point transition from congestion to frozen-topological domains.
* `pottier-fdr` — **non-Markovian FDR (Pottier)** · imports: Caputo $\beta$ = aging slope in non-Markovian FDR · handle: the $\alpha_s=\beta$ leg · adds: the FDR/memory register of the data-collapse claim.

## Topological memory
* `topological-memory` — **topological / fault-tolerant quantum memory; surface code (Kitaev; Fowler et al.)** · imports: a protected logical degree of freedom, free to store and costly to address; threshold behaviour · handle: the protected-bit reference class; the surface-code substrate · adds: the surface-code near→below-threshold migration as the load-bearing positive instance.

## Winding / circulation detection
* `winding-spitzer` — **planar-Brownian winding (Spitzer)** · imports: winding angle $\to$ Cauchy, near-origin-dominated · handle: chirality detection · adds: read globally, not via the divergent local angular clock.
* `skew-product` — **skew-product diffusion (Itô–McKean)** · imports: planar diffusion = radial Bessel ⊗ angular clock; the origin polar (never attained) · handle: the achiral point as a polar crossing · adds: consistency with the open-interval discipline (the zero is never attained).
* `levy-area` — **stochastic area / rough paths (Lévy; Lyons)** · imports: $\tfrac12\oint(x\,dy-y\,dx)$, singularity-free global winding · handle: the chiral sign $=\mathrm{sign}(\mathrm{Im}\,\lambda)$ / Lévy area · adds: the global-winding reading the open-interval discipline forces.

## Homochirality substrate
* `frank-autocatalysis` — **autocatalytic mirror-symmetry breaking (Frank)** · imports: homochirality as a maintained broken symmetry · handle: homochirality = an actively-maintained protected current · adds: handedness = the topological bit, not a frozen accident.
* `kondepudi` — **chiral symmetry breaking far from equilibrium (Kondepudi–Nelson)** · imports: bifurcation under a parity-distinguishing bias in a NESS · handle: the NESS chiral setting · adds: the drive-sweep falsifier framing.
* `kinetic-proofreading` — **kinetic proofreading (Hopfield; Ninio)** · imports: dissipative editing sustaining fidelity by burning drive · handle: the maintained mode (the fuel the falsifier titrates) · adds: the proofreading network as a protected-circulation carrier.

---

## TOOLKIT (untracked vocabulary — common nouns, not claims)

Generic mathematical language used freely and owned by no one: Laplacian, Jacobian,
eigenvalue/eigenvector/spectrum, Lyapunov function (the generic construction; the *specific*
Volterra–Lyapunov diagonal-stability result is tracked above), gradient, manifold, geodesic
(the object; the Fisher-manifold *framing* is `info-geometry`), linear stability analysis,
fixed point, limit cycle, attractor, the Langevin / Fokker–Planck / master-equation
formalisms, Fourier/Laplace transforms, correlation function, the chain rule of probability.
Using these claims nothing; they are the alphabet, not a result.

---

## RESIDUAL (what the framework owns — present here only by exclusion)

Not importable, hence not listed above (see [`character.md`](character.md) §"What the
translation leaves standing"): the **forcing** of every import onto the single parameter `a`
and the **over-determination** that makes the alignment a test; the umbrella term
**character**; the identification of the deformation algebra as the **tangent space of a
single morphospace** and the **proximity laws** (signs set by the Killing form; union-graph
frustration mints a protected current); the **binding** (graph-frustration ⟺ spectral complex
pair ⟺ cycle affinity) read in its falsifiable direction, with protection located in the
affinity; **identity** as the protected coordinates sustained as a circulation, and at the
composite level the minted, sustained bit; the **closure `⊗`** and its one real instance;
every *reading* (the "adds" column above); and every *falsifier*. The imports are the
load-bearing skeleton borrowed wholesale; the reading is the only thing claimed.
